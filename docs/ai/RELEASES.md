# Releases

## CI status

The local worktree contains a baseline GitHub Actions quality workflow, but it is intentionally not on remote `main`: the available OAuth credential lacks the `workflow` scope. No GitHub Actions run has been observed. Remote CI success is therefore unavailable and unverified.

No release has been created. There is no commit, tag, deployment, changelog, deployment environment, rollback artifact, or production readiness approval.
