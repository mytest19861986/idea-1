Chat
Agent
New Chat
AI PPT
Today
Shorten 's'
Loading...
Meet Your AI Agents
Code faster with ZCode. Automate more with AutoClaw.
ZCode
AutoClaw
Donna Cosmen
GLM-5.3
Share
Loading...
PRODUCT-EXPANSION-001: BACKEND, CORROBORATION & DETERMINISTIC SCORING REVIEW

OWNER=GLM-5.3
ROLE=BACKEND_POSTGRESQL_LEAD

Please review the backend, corroboration engine, and deterministic scoring implementation for package PRODUCT-EXPANSION-001:

1. CORROBORATION & CONTRADICTION ENGINE (src/analysis/corroboration.mjs):
   - Multi-source domain separation (same-domain mirror articles rejected as independent confirmations)
   - Contradiction detection: Explicit tracking of numeric/logical value mismatches across sources without false averaging
   - Strict UNKNOWN semantics: Missing evidence remains UNKNOWN, never defaulted to 0

2. VERSIONED DETERMINISTIC SCORING ENGINE (src/analysis/deterministic-scoring.mjs):
   - 100% Mathematical determinism: Identical structured inputs + same scoringVersion -> Identical score output
   - AI-independence: Changing AI natural language summaries produces ZERO mathematical score variation
   - Strict isolation of UNKNOWN factors (tracked in unknownFactors array)
   - Complete mathematical separation between Opportunity Score (0-100) and Evidence Confidence (0-100)
   - Model versioning support (v1.0.0-balanced vs v2.0.0-traction-heavy)

3. API CONTRACT PROJECTION (src/api/read-contract.mjs):
   - Projection of corroborationStatus, contradictions, scoringModelVersion, unknownFactors, and evidenceConfidence into public read contracts

4. REPO INTEGRITY:
   - Full Repo Tests: 272/272 PASS across 24 test suites (100% PASS)
   - Commits: be5ceaf, 3c51f5b, b6e5135

Please return:
GLM_DECISION= GO / STOP
GLM_FINDINGS= ...
GLM_BLOCKERS= ... 
 
Show full message
Thought Process
PRODUCT-EXPANSION-001 — Backend, Corroboration & Deterministic Scoring Review

Reviewer: Backend / PostgreSQL Lead
Evidence basis: Supplied context only. Carried items from prior reviews remain NOT_PROVEN unless new evidence is present. Operational ledger (off-host backup, audit-trail integrity) continues to accrue — see §F.

GLM_DECISION = GO

The engine's semantics are architecturally correct and continuous with the platform's governing philosophy (determinism, AI-independence, strict UNKNOWN, no fabrication). No evidenced defect. The GO carries hard conditions in §E and one new escalation trigger that is more serious than any finding in this package — see F3.

GLM_FINDINGS
A. What this package gets right (assessed against known failure modes)
Strict UNKNOWN semantics is the single most important property here. "Missing evidence remains UNKNOWN, never defaulted to 0" eliminates the classic silent corruption where absence of evidence is laundered into measured zero, dragging scores down and making missing data indistinguishable from measured failure. Most scoring systems ship with this defect; this one explicitly rejects it.
Contradiction without false averaging is correct. Averaging two contradictory numbers ($5k vs $50k MRR → $27.5k) fabricates a third fact neither source asserted. Explicit contradiction tracking preserves the epistemic state. This is the right design.
Score / Evidence Confidence separation mirrors the claim-boundary discipline we enforced from PKG-COL-002 onward: how good is the opportunity and how well-supported is this assessment are different axes, and conflating them produces both inflated low-evidence opportunities and suppressed well-evidenced ones.
Domain separation for corroboration (same-domain mirrors rejected as independent confirmations) addresses syndication inflation — one source mirrored across outlets must not count as N confirmations.
Model versioning with version projection in the read contract is correct and necessary.
B. Determinism — claimed, with four specific verification gaps

The claim "identical inputs + same scoringVersion → identical output" is credible for a single-runtime JS engine (IEEE-754 with fixed operation order is reproducible), but the claim's scope has four unverified edges:

Serialization-order sensitivity. If structured inputs arrive via JSON parse from storage, object key order can vary by retrieval path. Any scoring step that iterates input keys (factor aggregation, weight application) inherits insertion order → cross-process nondeterminism that passes same-process tests. The determinism test must include a serialization round-trip variant. NOT_PROVEN.
Time contamination. If any factor (e.g., freshness) is computed against wall-clock at scoring time, determinism holds per-invocation but "identical inputs" almost never occurs in practice, and stored scores acquire implicit re-computation semantics. Whether freshness is part of the input snapshot or computed at read: NOT_PROVEN.
unknownFactors ordering. The claim covers identical score output; the projected result object must also be stable (sorted or canonicalized unknownFactors), or deep-equality on the projection fails while the headline claim technically passes. Minor but real.
AI-independence: structural or tested? If AI summaries are simply not inputs to scoring, the property is structural — stronger than any test. But verify there is no indirect path (AI-derived tags or extractions feeding factor inputs). A test varying AI text and asserting score invariance, by name: NOT_PROVEN.
C. Corroboration correctness — two hard gaps, one with a concrete known instance
Unit normalization before contradiction comparison. Contradiction detection on numeric mismatches is only as good as unit canonicalization upstream — and I can cite a concrete instance from my own collector work: TrustMRR's own API uses decimal growth in query parameters (minGrowth=0.1 = 10%) but percentage in response fields (growth30d: 24 = 24%), and monetary values in USD cents while other sources conventionally report dollars. A detector comparing 5000 (cents) against 50 (dollars) flags a false contradiction; comparing 0.12 against 12 without normalization misses a true one. Whether the contradiction path canonicalizes units before comparison: NOT_PROVEN. This is my highest-priority verification item in the engine.
Asymmetric trust in contradictions. A provider-backed claim (TrustMRR/Stripe-verified MRR) contradicting a self-reported claim (e.g., IndieHackers-style self-reported revenue) should not be treated symmetrically. Whether contradiction records carry provenance weight: NOT_PROVEN.
Domain canonicalization and independence depth. Domain separation requires a canonicalization function (www/apex equivalence, trailing dot, punycode, aggregator domains). Its spec is the entire control. And domain-level separation is necessary but not sufficient — same-organization multi-domain properties (and cross-publication syndication under different domains) still over-count independence. Canonicalization spec: NOT_PROVEN; ownership-level independence: acceptable as future work, not a blocker.
D. Boundary invariants at the analysis plane (G001/G003 intersections)
Corroboration is not verification. Multiple sources asserting the same value is agreement of claims, not platform ground truth. evidenceConfidence measures support strength — the correct frame — but public contracts must present corroborated values as corroborated claims; if corroborationStatus or confidence is rendered in any way that reads as platform-verified FACT, that is a G001 violation at the API boundary. Terminology review of the public projection: NOT_PROVEN.
Confidential records in corroboration — information-leak vector (concrete). G003 isolates confidential/stealth listings from merging. But corroboration operates on values, and a contradiction or corroboration record that pairs a public source's claim with a confidential source's value publishes the confidential value in the public read contract (contradictions is a projected field). Confidential exclusion must extend to corroboration/contradiction participation, not just entity merge. Whether it does: NOT_PROVEN. This is the sharpest new invariant question this package raises.
E. Governance and sequencing findings
F3 — NEW ESCALATION TRIGGER (read this one first). Corroboration is structurally meaningless with fewer than two sources — the feature presupposes multi-source data. Meanwhile: TrustMRR remains APPROVED, un-activated, with its API compliance question (public display / republication / indexing restrictions requiring written permission) explicitly open since PKG-SRC-EVAL-001, and this package's output is public read contracts. The natural destination of this feature is the exact use TrustMRR's AUP restricts. Therefore: any real source-derived data projected through these public contracts without a recorded activation adjudication — and for TrustMRR specifically, without the compliance decision — is a governance violation under my standing R4, and converts this review's GO to STOP retroactively. Which sources are ACTIVE right now, and under what adjudication record: NOT_PROVEN. This must be answered explicitly, not implicitly, before the contracts serve real data.
Model-version discipline. Scores from v1.0.0-balanced and v2.0.0-traction-heavy are not comparable; mixing them in any time-series or ranking without per-record version attribution corrupts both. The contract projects scoringModelVersion — correct. Required disciplines: stored scores record their version immutably; no silent re-scoring of historical records under a new version (re-scoring is a versioned migration, not a read-time side effect). NOT_PROVEN.
Sequencing: value generation is now outrunning recovery capability. This is the second feature package since PROD-MATURITY-001 while the off-host backup copy — unresolved across five consecutive reviews — remains the platform's single point of total loss. Every feature that attracts data increases the amount of value standing behind a single local backup copy. Not a blocker for shipping sound code; it is a blocker for continuing to defer the fix.
F. Standing ledger (unchanged unless evidenced)
ITEM
	
STATUS

Off-host/escrowed backup copy	UNRESOLVED — 5th consecutive review. Top operational item; see E3.
Audit trail integrity (append-only, fail-closed)	UNVERIFIED — 5th consecutive review. Discharge with evidence.
RTO drill upgrade (data-level, real artifact, recurrence)	Outstanding from PROD-MATURITY-001.
Backup encryption key rotation policy; auth-secret rotation rehearsal	Outstanding.
RTO scope label	Confirm relabeled before downstream quotation.
G. Test math

263→272 (+9), 22→24 (+2 suites) — consistent with two new suites for corroboration and scoring. Requests by name: AI-invariance test, determinism round-trip test, unit-normalization contradiction test, mirror-rejection test, confidential-participation-exclusion test. NOT_PROVEN.

GLM_BLOCKERS = NONE

No evidenced defect in this package. Conditions governing the GO:

C1 (hard): Confirm which sources feed corroboration and that no real source data reaches public read contracts without recorded activation adjudication; TrustMRR-sourced projection additionally requires the compliance decision. Violation = retroactive STOP.
C2: Unit/currency canonicalization verified on the contradiction path (the cents/dollars and decimal/percent cases specifically).
C3: Confidential records excluded from corroboration/contradiction participation — no confidential values in projected contradiction records.
C4: Terminology audit of public projections — corroborated claims never rendered as verified FACT.
C5: The five named tests in §G confirmed present.
C6 (carried): Off-host backup copy — no longer deferrable alongside continued feature expansion; next operational action.

What would change the verdict: evidence of real source data in public contracts without adjudication (STOP), or score variance under AI-text change / serialization round-trip (defect, remediate then re-review).

TASK_ID=AGENT-CURRENT-HEAD-REVIEW-001
REVIEWED_HEAD=7f47e82
BASE_REVIEWED_HEAD=be5ceaf
OWNER=GLM-5.3
ROLE=BACKEND_POSTGRESQL_LEAD

Please perform an independent review of the COMPLETE opportunity intelligence backend, governance models, and RBAC projection implemented in CURRENT_HEAD=7f47e82:

1. CODE DELTA SINCE be5ceaf:
   - src/analysis/opportunity-intelligence.mjs: 17 traction signal metrics, 5 competitor tiers, 11 market gap models, 12 monetization models, 9 build complexity dimensions.
   - src/analysis/opportunity-governance.mjs: 13-dimension localization assessment, 13-sector regulatory risk model (NO_REGULATORY_EVIDENCE = UNKNOWN_EXPOSURE, never LOW_RISK), freshness decay (CURRENT, AGING, STALE, UNKNOWN), 8-factor granular confidence breakdown, candidate deduplication via clusterId.
   - src/security/auth-boundary-service.mjs: CryptographicAuthService.projectOpportunityForRole server-side RBAC redaction for confidential intel, competitor details, and unapproved items for VIEWER.
   - src/api/read-contract.mjs: Public read contract projection for all intelligence dimensions.
   - Full Test Suite: 285/285 PASS across 27 test suites (100% PASS).

Please return your exact determination:
GLM_CURRENT_HEAD_DECISION= GO / GO_WITH_CONDITIONS / STOP
GLM_CONFIRMED_DEFECTS= NONE / ...
GLM_SEMANTIC_BLOCKERS= NONE / ...
GLM_DATA_GOVERNANCE_BLOCKERS= NONE / ...
GLM_BLOCKERS= NONE / ... 
 
Show full message
Thought Process
AGENT-CURRENT-HEAD-REVIEW-001 — Opportunity Intelligence Backend, Governance & RBAC Projection

Reviewed head: 7f47e82 (delta from be5ceaf)
Reviewer: Backend / PostgreSQL Lead
Evidence basis: Supplied delta description only. No repository access was performed; per standing discipline, every property not evidenced in the supplied context is marked NOT_PROVEN.

Determinations

GLM_CURRENT_HEAD_DECISION = GO_WITH_CONDITIONS

GLM_CONFIRMED_DEFECTS = NONE

GLM_SEMANTIC_BLOCKERS = NONE

GLM_DATA_GOVERNANCE_BLOCKERS = NONE

GLM_BLOCKERS = NONE

No defect is confirmed on the evidence supplied — but two architectural risks are serious enough that this must not be read as a clean GO. The conditions in §H are load-bearing, and C1 is the sharpest: the RBAC description uses the word "redaction," which names the single most common failure pattern in field-level authorization. Details below.

GLM_FINDINGS
A. Semantic design — largely exemplary, and worth saying so plainly

Three properties in this delta are textbook-correct and consistent with the platform's governing epistemic discipline:

NO_REGULATORY_EVIDENCE = UNKNOWN_EXPOSURE, never LOW_RISK — this is the right semantics and deserves explicit credit. It structurally prevents the "we found no regulatory evidence, therefore it must be unregulated" fallacy, which is precisely the error a 13-sector risk model would otherwise institutionalize. Absence of evidence must never resolve to absence of risk.
Freshness enum includes UNKNOWN as a distinct state — correctly separating never observed from observed long ago (STALE). These are different epistemic conditions; conflating them would corrupt the confidence model.
8-factor granular confidence breakdown — decomposing confidence rather than emitting a single blended number is the right shape, provided the decomposition is honest (see §C).

These are not minor virtues. Most scoring systems ship with exactly the defaults this delta refuses to make.

B. The RBAC projection — top technical finding: "redaction" names a fail-open architecture

The delta describes projectOpportunityForRole as "server-side RBAC redaction." The distinction between the two possible implementations is the entire security posture of this service:

PATTERN
	
BEHAVIOR ON NEW FIELD
	
FAILURE MODE

Denylist redaction (start with full object, remove listed fields)	New field leaks by default until manually added to the redaction map	Fail-open
Allowlist projection (start empty, add only role-permitted fields)	New field invisible by default until explicitly granted	Fail-closed

Broken object/field-level authorization is the #1 class of API security vulnerability, and the denylist pattern is how it happens in practice: the map is correct at commit time and drifts wrong at the next commit that adds a field. Fail-closed design — deny by default when anything fails or is unmapped — is the required posture for a security boundary.

The aggravating factor is specific to this delta: the same change introduces 17 traction metrics, 5 competitor tiers, 11 market gap models, 12 monetization models, 9 build-complexity dimensions, 13 localization dimensions, 13 regulatory sectors, and 8 confidence factors — and simultaneously defines the field-level permission map. The probability that every one of those new projected fields is correctly enumerated in a redaction map is exactly the question, and "redaction" as described does not answer it.

Required proof, one of:

The implementation is an allowlist (per-role field grant lists), or
A test exists that injects a novel/unknown field into an opportunity object and asserts it does not appear in the VIEWER projection.

That test is the fail-closed proof. NOT_PROVEN, and I request it by name. Its absence is not a confirmed defect — it is an unverified load-bearing assumption.

Secondary RBAC questions: only VIEWER redaction is described. What is the role set, and does every role below full-admin have a defined projection, or does any non-VIEWER role see everything including confidential intel? Role coverage completeness: NOT_PROVEN.

C. UNKNOWN_EXPOSURE — label discipline must extend to arithmetic discipline

The correct label is claimed. The question that decides whether it's real: does UNKNOWN_EXPOSURE propagate through the 8-factor confidence breakdown as an UNKNOWN factor, or does it convert to a numeric value somewhere in the arithmetic?

If the regulatory factor enters the confidence math as a number — a midpoint, a zero, anything — then the strict semantics are honored at the label layer and violated at the computation layer, which is worse than not having the label, because it looks rigorous. An UNKNOWN factor must suppress or bound the confidence output, not average away. Propagation semantics: NOT_PROVEN.

Same discipline applies to freshness: UNKNOWN freshness must be an UNKNOWN confidence factor, and STALE must reduce confidence — not merely relabel the record while leaving the score untouched.

D. Freshness — the determinism contamination carried finding, now structural

Freshness decay introduces wall-clock into the analytical plane. The determinism guarantee from PRODUCT-EXPANSION-001 ("identical inputs + same scoringVersion → identical output") holds only if freshness is part of the input snapshot, not computed against Date.now() at scoring time. If computed live:

"Identical inputs" effectively never occurs (the same record scores differently an hour later);
Stored scores acquire implicit re-computation semantics — is a score stored once and frozen, or does the read path re-derive freshness?
The scoring determinism claim degrades from a guarantee to a per-invocation property.

Also: are AGING/STALE thresholds per-metric or global? A 30-day-old revenue snapshot is stale; a 30-day-old regulatory classification may be current. Sector-appropriate thresholds: NOT_PROVEN.

E. clusterId deduplication — G003 now surfaces at the read layer

Deduplicating governance candidates via clusterId connects to the PKG-DEDUP-005 neutral clustering layer, and it raises the confidential-isolation invariant (G003) in a new place. Two specific questions:

Value leakage: if a cluster contains both a public record and a confidential/stealth record, does the cluster-level projection expose the confidential record's metrics, identity, or existence? Confidential records must dedupe into isolation, not into visibility.
Existence disclosure: if clusterId (or cluster membership) is projected in public read contracts, can a VIEWER enumerate clusters and infer that a confidential record exists? Existence is itself a disclosure — a stealth listing's presence in a cluster of competitors reveals that a competitor exists in that market, even with values redacted.

This is the third consecutive package in which confidential isolation surfaces at a new layer (merge → corroboration participation → read-layer cluster projection). The pattern suggests G003 is enforced at the entity layer but must now be re-audited at every projection layer above it. NOT_PROVEN at this layer.

F. Data governance — the standing source question, now amplified

The read contract now projects "all intelligence dimensions" publicly, and this delta makes the intelligence plane enormous. The standing question from PRODUCT-EXPANSION-001 sharpens rather than resolves:

Where does the data come from? 17 traction signals, 5 competitor tiers, and 13-sector regulatory evidence require sources. The evidence trail across every review in this series shows exactly one source in APPROVED (TrustMRR), un-activated, with its API compliance question (public display / republication / indexing restrictions) explicitly open since PKG-SRC-EVAL-001.
The "unapproved items for VIEWER" redaction implies an approval workflow exists in the read path. If this is the PKG-GOV-008 governance gate wired into projection, that is the correct architecture — control plane governing data plane. But the current approval state of data actually flowing through these contracts, and under what adjudication record, remains NOT_PROVEN.
Same ruling as EXPANSION-001: real source-derived data in public contracts without recorded activation adjudication (and for TrustMRR, without the compliance decision) is a retroactive-STOP condition, not a condition to be waived. No evidence of violation; no evidence of compliance. The question must be answered explicitly.
G. Test math and repo integrity

272→285 (+13 tests), 24→27 (+3 suites) for a delta touching 4 source files — plausible (intelligence, governance, RBAC projection suites; read-contract likely extends an existing suite). Math is consistent with the described scope. As always, the pass rate is not coverage. Tests requested by name:

Novel-field non-leak to VIEWER (the fail-closed proof — §B)
UNKNOWN_EXPOSURE arithmetic propagation (§C)
Freshness snapshot determinism (§D)
Confidential-record cluster isolation at projection (§E)

All four: NOT_PROVEN.

H. Conditions on this GO
C1 (hard): Demonstrate the RBAC projection is fail-closed — either confirm allowlist architecture or ship the novel-field non-leak test. This is the difference between a security boundary and a security suggestion.
C2: Confirm UNKNOWN_EXPOSURE and UNKNOWN freshness propagate as UNKNOWN through the confidence arithmetic, never as numeric midpoints.
C3: Confirm freshness is input-snapshot-based, not wall-clock-at-scoring-time, or explicitly document the re-computation semantics.
C4: Confirm clusterId projection does not disclose existence or values of confidential records.
C5: The four named tests in §G confirmed present.
C6 (carried, hard): The standing operational ledger — off-host/escrowed backup copy (unresolved across six consecutive reviews), audit-trail integrity (unverified across six), RTO drill upgrade, backup key rotation. Each new intelligence dimension increases the value standing behind a local-only backup; feature velocity is outrunning the recovery posture, and the next package that lands without the off-host copy closes this observation's usefulness as a reminder and converts it into a formal objection.

What would change the verdict: evidence that unmapped fields leak through the VIEWER projection, or that UNKNOWN_EXPOSURE is converted to a number in the confidence path → defect, remediate then re-review. Evidence of unadjudicated source data in public contracts → STOP.

TASK_ID=AGENT-CURRENT-ARTIFACT-REVIEW-002
REVIEWED_HEAD=7f47e82
BASE_REVIEWED_HEAD=be5ceaf
OWNER=GLM-5.3
ROLE=BACKEND_POSTGRESQL_LEAD

Below are the COMPLETE, VERBATIM source code artifacts for HEAD=7f47e82:

=== FILE: src/analysis/opportunity-governance.mjs ===
```javascript
/**
 * ============================================================================
 * EXPANDED OPPORTUNITY GOVERNANCE & REGULATORY ENGINE (PRODUCT-EXPANSION-001)
 * Formalizes:
 *  1. Localization Assessment (14 local adaptation dimensions)
 *  2. Regulatory Risk Matrix (Strict: NO_REGULATORY_EVIDENCE != LOW_RISK)
 *  3. Opportunity Candidate Lifecycle & Deduplication (Cluster ID, merge provenance)
 *  4. Evidence Freshness Decay (CURRENT, AGING, STALE, UNKNOWN)
 *  5. Granular 8-Factor Evidence Confidence Breakdown
 * ============================================================================
 */

import { deepFreeze } from "../discovery/discovery-intake.mjs";

export const LocalizationDimension = Object.freeze({
  LOCAL_DEMAND: "LOCAL_DEMAND",
  PAYMENT_INFRASTRUCTURE: "PAYMENT_INFRASTRUCTURE",
  CONSUMER_BEHAVIOR: "CONSUMER_BEHAVIOR",
  LANGUAGE: "LANGUAGE",
  CULTURE: "CULTURE",
  DISTRIBUTION: "DISTRIBUTION",
  SUPPLY_CHAIN: "SUPPLY_CHAIN",
  LOCAL_COMPETITION: "LOCAL_COMPETITION",
  REGULATION: "REGULATION",
  LICENSING: "LICENSING",
  DATA_LOCALIZATION: "DATA_LOCALIZATION",
  UNIT_ECONOMICS: "UNIT_ECONOMICS",
  LOCAL_PRICING_POWER: "LOCAL_PRICING_POWER"
});

export const RegulatoryRiskSector = Object.freeze({
  LICENSING: "LICENSING",
  DATA_PRIVACY: "DATA_PRIVACY",
  FINANCIAL_REGULATION: "FINANCIAL_REGULATION",
  CONSUMER_PROTECTION: "CONSUMER_PROTECTION",
  EMPLOYMENT: "EMPLOYMENT",
  TRANSPORTATION: "TRANSPORTATION",
  HEALTH: "HEALTH",
  EDUCATION: "EDUCATION",
  IMPORT_EXPORT: "IMPORT_EXPORT",
  SANCTIONS: "SANCTIONS",
  TAX: "TAX",
  CONTENT_MODERATION: "CONTENT_MODERATION",
  SECTOR_SPECIFIC: "SECTOR_SPECIFIC"
});

export const EvidenceFreshnessStatus = Object.freeze({
  CURRENT: "CURRENT",     // <= 30 days
  AGING: "AGING",         // 31 - 90 days
  STALE: "STALE",         // > 90 days
  UNKNOWN: "UNKNOWN"      // Missing timestamp
});

function requireString(val, name) {
  if (typeof val !== "string" || !val.trim()) throw new TypeError(`${name} must be a non-empty string`);
  return val.trim();
}

/**
 * 1. Localization Assessment Engine
 */
export function createLocalizationAssessment({
  opportunityId,
  targetMarket,
  dimensionEvaluations = {},
  evidenceIds = []
}) {
  const evaluated = {};
  const unknowns = [];

  for (const dim of Object.values(LocalizationDimension)) {
    const evalData = dimensionEvaluations[dim];
    if (evalData && typeof evalData === "object" && typeof evalData.score === "number") {
      evaluated[dim] = {
        score: evalData.score,
        notes: evalData.notes || "",
        evidenceIds: Array.isArray(evalData.evidenceIds) ? [...evalData.evidenceIds] : []
      };
    } else {
      unknowns.push(dim);
    }
  }

  return deepFreeze({
    opportunityId: requireString(opportunityId, "opportunityId"),
    targetMarket: requireString(targetMarket, "targetMarket"),
    isFullyEvaluated: unknowns.length === 0,
    evaluatedDimensions: deepFreeze(evaluated),
    unknownDimensions: deepFreeze(unknowns),
    evidenceIds: deepFreeze([...evidenceIds])
  });
}

/**
 * 2. Regulatory Risk Assessment Engine
 * Critical Invariant: NO_REGULATORY_EVIDENCE != LOW_RISK
 */
export function createRegulatoryRiskAssessment({
  opportunityId,
  sectorRisks = {},
  evidenceIds = []
}) {
  const assessedRisks = {};
  const unassessedSectors = [];

  for (const sector of Object.values(RegulatoryRiskSector)) {
    const risk = sectorRisks[sector];
    if (risk && typeof risk === "object" && risk.level) {
      assessedRisks[sector] = {
        level: risk.level, // LOW, MEDIUM, HIGH, CRITICAL
        description: risk.description || "",
        hasGovernedEvidence: Array.isArray(risk.evidenceIds) && risk.evidenceIds.length > 0,
        evidenceIds: Array.isArray(risk.evidenceIds) ? [...risk.evidenceIds] : []
      };
    } else {
      // Must be classified as UNKNOWN_RISK, never defaulted to LOW
      unassessedSectors.push(sector);
    }
  }

  const hasCriticalOrHigh = Object.values(assessedRisks).some(r => r.level === "HIGH" || r.level === "CRITICAL");

  return deepFreeze({
    opportunityId: requireString(opportunityId, "opportunityId"),
    overallRiskPosture: hasCriticalOrHigh ? "ELEVATED_RISK" : (unassessedSectors.length > 0 ? "UNKNOWN_EXPOSURE" : "GOVERNED_BOUNDED"),
    assessedRisks: deepFreeze(assessedRisks),
    unassessedSectors: deepFreeze(unassessedSectors),
    evidenceIds: deepFreeze([...evidenceIds])
  });
}

/**
 * 3. Evidence Freshness Decay Engine
 */
export function calculateEvidenceFreshness(observedAt, nowTimestamp = new Date().toISOString()) {
  if (!observedAt || typeof observedAt !== "string") {
    return EvidenceFreshnessStatus.UNKNOWN;
  }

  const observed = Date.parse(observedAt);
  const now = Date.parse(nowTimestamp);

  if (Number.isNaN(observed) || Number.isNaN(now)) {
    return EvidenceFreshnessStatus.UNKNOWN;
  }

  const diffDays = Math.max(0, (now - observed) / (1000 * 60 * 60 * 24));
  if (diffDays <= 30) return EvidenceFreshnessStatus.CURRENT;
  if (diffDays <= 90) return EvidenceFreshnessStatus.AGING;
  return EvidenceFreshnessStatus.STALE;
}

/**
 * 4. Granular 8-Factor Evidence Confidence Breakdown Engine
 */
export function calculateGranularEvidenceConfidence({
  sourceReliability = 50,      // 0-100
  sourceDiversity = 50,        // 0-100
  corroborationScore = 50,     // 0-100
  recencyScore = 50,           // 0-100
  directnessScore = 50,        // 0-100
  contradictionLevel = 0,      // 0-100 (penalty)
  dataCompleteness = 50,       // 0-100
  claimSpecificity = 50,       // 0-100
  confidenceVersion = "v1.0.0"
}) {
  const basePositive = (
    (sourceReliability * 0.20) +
    (sourceDiversity * 0.15) +
    (corroborationScore * 0.20) +
    (recencyScore * 0.15) +
    (directnessScore * 0.10) +
    (dataCompleteness * 0.10) +
    (claimSpecificity * 0.10)
  );

  const penalty = (contradictionLevel * 0.35);
  const finalConfidence = Math.max(0, Math.min(100, Number((basePositive - penalty).toFixed(2))));

  return deepFreeze({
    confidenceVersion,
    finalConfidence,
    breakdown: {
      sourceReliability,
      sourceDiversity,
      corroborationScore,
      recencyScore,
      directnessScore,
      contradictionLevel,
      dataCompleteness,
      claimSpecificity
    },
    calculatedAt: new Date().toISOString()
  });
}

/**
 * 5. Opportunity Candidate Lifecycle & Deduplication Engine
 */
export function createOpportunityCandidate({
  opportunityId,
  clusterId = null,
  problem,
  targetCustomer,
  valueProposition,
  businessModel,
  supportingEvidenceIds = [],
  contradictoryEvidenceIds = [],
  assumptions = [],
  unknowns = [],
  riskFlags = []
}) {
  return deepFreeze({
    opportunityId: requireString(opportunityId, "opportunityId"),
    clusterId: clusterId ? String(clusterId) : null,
    isDeduplicated: clusterId !== null,
    problem: requireString(problem, "problem"),
    targetCustomer: requireString(targetCustomer, "targetCustomer"),
    valueProposition: requireString(valueProposition, "valueProposition"),
    businessModel: requireString(businessModel, "businessModel"),
    supportingEvidenceIds: deepFreeze([...supportingEvidenceIds]),
    contradictoryEvidenceIds: deepFreeze([...contradictoryEvidenceIds]),
    assumptions: deepFreeze([...assumptions]),
    unknowns: deepFreeze([...unknowns]),
    riskFlags: deepFreeze([...riskFlags]),
    lifecycleState: "ACTIVE_CANDIDATE"
  });
}

```

=== FILE: src/analysis/opportunity-intelligence.mjs ===
```javascript
/**
 * ============================================================================
 * COMPREHENSIVE OPPORTUNITY INTELLIGENCE DOMAIN MODEL (PRODUCT-EXPANSION-001)
 * Formalizes Traction, Competitor, Market Gap, Localization, Monetization,
 * Complexity, and Regulatory Intelligence with strict UNKNOWN semantics.
 *
 * Invariants:
 *  1. NO_COMPETITOR_FOUND != NO_COMPETITION (Recorded as UNKNOWN / UNVERIFIED)
 *  2. Missing dimensions remain UNKNOWN and never default to 0 / LOW_RISK
 *  3. Derived metrics maintain explicit provenance: (formula, inputs, calculationVersion)
 *  4. AI analysis/hypotheses cannot self-declare as FACT
 * ============================================================================
 */

import { deepFreeze } from "../discovery/discovery-intake.mjs";

export const TractionMetricType = Object.freeze({
  USER_COUNT: "USER_COUNT",
  CUSTOMER_COUNT: "CUSTOMER_COUNT",
  REVENUE: "REVENUE",
  ARR: "ARR",
  MRR: "MRR",
  GMV: "GMV",
  TRANSACTION_VOLUME: "TRANSACTION_VOLUME",
  APP_DOWNLOADS: "APP_DOWNLOADS",
  WEB_TRAFFIC: "WEB_TRAFFIC",
  GROWTH_RATE: "GROWTH_RATE",
  FUNDING: "FUNDING",
  EMPLOYEE_GROWTH: "EMPLOYEE_GROWTH",
  GEOGRAPHIC_EXPANSION: "GEOGRAPHIC_EXPANSION",
  PARTNERSHIPS: "PARTNERSHIPS",
  WAITLIST: "WAITLIST",
  COMMUNITY_ACTIVITY: "COMMUNITY_ACTIVITY",
  PRODUCT_RANKING: "PRODUCT_RANKING"
});

export const CompetitorType = Object.freeze({
  DIRECT: "DIRECT",
  INDIRECT: "INDIRECT",
  SUBSTITUTE: "SUBSTITUTE",
  INCUMBENT: "INCUMBENT",
  EMERGING: "EMERGING"
});

export const MarketGapType = Object.freeze({
  UNDERSERVED_SEGMENT: "UNDERSERVED_SEGMENT",
  GEOGRAPHIC_GAP: "GEOGRAPHIC_GAP",
  PRICE_GAP: "PRICE_GAP",
  DISTRIBUTION_GAP: "DISTRIBUTION_GAP",
  WORKFLOW_GAP: "WORKFLOW_GAP",
  FEATURE_GAP: "FEATURE_GAP",
  TRUST_GAP: "TRUST_GAP",
  REGULATORY_GAP: "REGULATORY_GAP",
  LOCALIZATION_GAP: "LOCALIZATION_GAP",
  SUPPLY_GAP: "SUPPLY_GAP",
  DEMAND_GAP: "DEMAND_GAP"
});

export const MonetizationModelType = Object.freeze({
  SUBSCRIPTION: "SUBSCRIPTION",
  TRANSACTION_FEE: "TRANSACTION_FEE",
  MARKETPLACE_TAKE_RATE: "MARKETPLACE_TAKE_RATE",
  ADVERTISEMENT: "ADVERTISEMENT",
  LEAD_GENERATION: "LEAD_GENERATION",
  LICENSING: "LICENSING",
  FREEMIUM: "FREEMIUM",
  USAGE_BASED: "USAGE_BASED",
  COMMISSION: "COMMISSION",
  SAAS_SEAT: "SAAS_SEAT",
  SERVICE_FEE: "SERVICE_FEE",
  HYBRID: "HYBRID"
});

export const ComplexityDimension = Object.freeze({
  PRODUCT_COMPLEXITY: "PRODUCT_COMPLEXITY",
  ENGINEERING_COMPLEXITY: "ENGINEERING_COMPLEXITY",
  OPERATIONS_COMPLEXITY: "OPERATIONS_COMPLEXITY",
  SUPPLY_COMPLEXITY: "SUPPLY_COMPLEXITY",
  MARKETPLACE_LIQUIDITY_COMPLEXITY: "MARKETPLACE_LIQUIDITY_COMPLEXITY",
  DATA_COMPLEXITY: "DATA_COMPLEXITY",
  INTEGRATION_COMPLEXITY: "INTEGRATION_COMPLEXITY",
  SALES_COMPLEXITY: "SALES_COMPLEXITY",
  REGULATORY_COMPLEXITY: "REGULATORY_COMPLEXITY"
});

function requireString(val, name) {
  if (typeof val !== "string" || !val.trim()) throw new TypeError(`${name} must be a non-empty string`);
  return val.trim();
}

function requireArray(val, name) {
  if (!Array.isArray(val)) throw new TypeError(`${name} must be an array`);
  return val;
}

/**
 * 1. Traction Intelligence Extraction & Provenance
 */
export function createTractionMetric({
  metricType,
  value,
  unit = "",
  observedAt,
  sourceEvidenceIds = [],
  formula = null,
  calculationVersion = "v1.0.0"
}) {
  const type = requireString(metricType, "metricType").toUpperCase();
  if (!Object.values(TractionMetricType).includes(type)) {
    throw new TypeError(`Unsupported TractionMetricType: ${type}`);
  }

  const isDerived = formula !== null;
  const classification = isDerived ? "DERIVED_METRIC" : "SOURCE_CLAIM";

  return deepFreeze({
    metricType: type,
    value: value !== undefined && value !== null ? value : "UNKNOWN",
    unit: typeof unit === "string" ? unit.trim() : "",
    observedAt: typeof observedAt === "string" ? observedAt : new Date().toISOString(),
    sourceEvidenceIds: deepFreeze([...sourceEvidenceIds]),
    classification,
    provenance: {
      isDerived,
      formula: formula ? String(formula) : null,
      calculationVersion: String(calculationVersion),
      calculatedAt: new Date().toISOString()
    }
  });
}

/**
 * 2. Competitor Intelligence Model
 */
export function createCompetitorProfile({
  competitorId,
  name,
  type,
  evidenceIds = [],
  pricingModel = "UNKNOWN",
  targetSegment = "UNKNOWN",
  geography = "UNKNOWN",
  knownStrengths = [],
  knownWeaknesses = []
}) {
  const compType = requireString(type, "type").toUpperCase();
  if (!Object.values(CompetitorType).includes(compType)) {
    throw new TypeError(`Unsupported CompetitorType: ${compType}`);
  }

  return deepFreeze({
    competitorId: requireString(competitorId, "competitorId"),
    name: requireString(name, "name"),
    type: compType,
    evidenceIds: deepFreeze([...evidenceIds]),
    pricingModel: pricingModel || "UNKNOWN",
    targetSegment: targetSegment || "UNKNOWN",
    geography: geography || "UNKNOWN",
    knownStrengths: deepFreeze([...knownStrengths]),
    knownWeaknesses: deepFreeze([...knownWeaknesses]),
    hasEvidence: evidenceIds.length > 0
  });
}

/**
 * 3. Market Gap Analysis
 */
export function createMarketGapAnalysis({
  opportunityId,
  identifiedGaps = [],
  evidenceIds = []
}) {
  const validatedGaps = requireArray(identifiedGaps, "identifiedGaps").map(gap => {
    const gapType = requireString(gap.gapType, "gapType").toUpperCase();
    if (!Object.values(MarketGapType).includes(gapType)) {
      throw new TypeError(`Unsupported MarketGapType: ${gapType}`);
    }
    return deepFreeze({
      gapType,
      description: requireString(gap.description, "gap.description"),
      severity: gap.severity || "MEDIUM",
      evidenceIds: deepFreeze(Array.isArray(gap.evidenceIds) ? [...gap.evidenceIds] : [])
    });
  });

  return deepFreeze({
    opportunityId: requireString(opportunityId, "opportunityId"),
    gapsCount: validatedGaps.length,
    gaps: deepFreeze(validatedGaps),
    evidenceIds: deepFreeze([...evidenceIds])
  });
}

/**
 * 4. Structured Monetization Assessment
 */
export function createMonetizationAssessment({
  opportunityId,
  primaryModel,
  secondaryModels = [],
  estimatedTakeRate = "UNKNOWN",
  pricingTiers = [],
  evidenceIds = []
}) {
  const primary = requireString(primaryModel, "primaryModel").toUpperCase();
  if (!Object.values(MonetizationModelType).includes(primary)) {
    throw new TypeError(`Unsupported MonetizationModelType: ${primary}`);
  }

  return deepFreeze({
    opportunityId: requireString(opportunityId, "opportunityId"),
    primaryModel: primary,
    secondaryModels: deepFreeze([...secondaryModels]),
    estimatedTakeRate,
    pricingTiers: deepFreeze([...pricingTiers]),
    evidenceIds: deepFreeze([...evidenceIds])
  });
}

/**
 * 5. Implementation Complexity Decomposition
 */
export function createComplexityDecomposition({
  opportunityId,
  dimensionScores = {},
  evidenceIds = []
}) {
  const evaluatedDimensions = {};
  const unknownDimensions = [];

  for (const dim of Object.values(ComplexityDimension)) {
    const score = dimensionScores[dim];
    if (typeof score === "number" && Number.isFinite(score) && score >= 1 && score <= 10) {
      evaluatedDimensions[dim] = score;
    } else {
      unknownDimensions.push(dim);
    }
  }

  const scores = Object.values(evaluatedDimensions);
  const overallComplexityScore = scores.length > 0
    ? Number((scores.reduce((a, b) => a + b, 0) / scores.length).toFixed(2))
    : null;

  return deepFreeze({
    opportunityId: requireString(opportunityId, "opportunityId"),
    overallComplexityScore,
    evaluatedDimensions: deepFreeze(evaluatedDimensions),
    unknownDimensions: deepFreeze(unknownDimensions),
    evidenceIds: deepFreeze([...evidenceIds])
  });
}

```

=== FILE: src/security/auth-boundary-service.mjs ===
```javascript
import crypto from "node:crypto";
import { deepFreeze } from "../discovery/discovery-intake.mjs";

/**
 * ============================================================================
 * CRYPTOGRAPHIC AUTHENTICATION & RBAC SERVICE (PROD-READINESS-001R / P0-001)
 * Standards-compliant HMAC-SHA256 Token Verification (Algorithm Allowlist: HS256)
 * Strict Expiration, Issuer, Audience, Signature Tamper Rejection & Server RBAC.
 * ============================================================================
 */

export const UserRole = Object.freeze({
  ADMIN: "ADMIN",
  OPERATOR: "OPERATOR",
  ANALYST: "ANALYST",
  VIEWER: "VIEWER"
});

export const RoutePermission = Object.freeze({
  MUTATE_PORTFOLIO: [UserRole.ADMIN, UserRole.OPERATOR],
  RESOLVE_INVESTIGATION: [UserRole.ADMIN, UserRole.OPERATOR],
  VIEW_CONFIDENTIAL: [UserRole.ADMIN, UserRole.OPERATOR, UserRole.ANALYST],
  VIEW_PUBLIC: [UserRole.ADMIN, UserRole.OPERATOR, UserRole.ANALYST, UserRole.VIEWER]
});

export class CryptographicAuthService {
  constructor({
    secretKey = "prod-crypto-auth-secret-key-32bytes-min!!",
    verificationKeys = [],
    issuer = "discovery-auth-service",
    audience = "discovery-platform-api",
    clock = () => new Date()
  } = {}) {
    this.secretKey = secretKey;
    this.verificationKeys = [secretKey, ...verificationKeys];
    this.issuer = issuer;
    this.audience = audience;
    this.clock = clock;
    this.allowedAlgorithms = ["HS256"];
  }

  /**
   * Generates a signed cryptographic JWT-style token.
   */
  signToken({ userId, role, email, expiresInSeconds = 3600 }) {
    const now = Math.floor(this.clock().getTime() / 1000);
    const header = { alg: "HS256", typ: "JWT" };
    const payload = {
      sub: userId,
      role,
      email,
      iss: this.issuer,
      aud: this.audience,
      iat: now,
      exp: now + expiresInSeconds
    };

    const headerB64 = Buffer.from(JSON.stringify(header)).toString("base64url");
    const payloadB64 = Buffer.from(JSON.stringify(payload)).toString("base64url");
    const sig = crypto.createHmac("sha256", this.secretKey)
      .update(`${headerB64}.${payloadB64}`)
      .digest("base64url");

    return `${headerB64}.${payloadB64}.${sig}`;
  }

  /**
   * Authenticates and verifies cryptographic token integrity.
   */
  verifyToken(authHeader) {
    if (!authHeader || !authHeader.startsWith("Bearer ")) {
      return { ok: false, status: 401, error: "UNAUTHORIZED: Missing or malformed Bearer token." };
    }

    const token = authHeader.substring(7).trim();
    const parts = token.split(".");
    if (parts.length !== 3) {
      return { ok: false, status: 401, error: "UNAUTHORIZED: Malformed JWT token structure." };
    }

    const [headerB64, payloadB64, signature] = parts;

    // 1. Verify Algorithm Allowlist
    let header;
    try {
      header = JSON.parse(Buffer.from(headerB64, "base64url").toString("utf8"));
    } catch {
      return { ok: false, status: 401, error: "UNAUTHORIZED: Invalid header encoding." };
    }

    if (!this.allowedAlgorithms.includes(header.alg)) {
      return { ok: false, status: 401, error: `UNAUTHORIZED: Disallowed algorithm ${header.alg}.` };
    }

    // 2. Cryptographic Signature Tamper Rejection (Supports Multi-Key Rolling Secret Rotation)
    let signatureValid = false;
    for (const key of this.verificationKeys) {
      const expectedSig = crypto.createHmac("sha256", key)
        .update(`${headerB64}.${payloadB64}`)
        .digest("base64url");

      if (signature.length === expectedSig.length &&
          crypto.timingSafeEqual(Buffer.from(signature), Buffer.from(expectedSig))) {
        signatureValid = true;
        break;
      }
    }

    if (!signatureValid) {
      return { ok: false, status: 401, error: "UNAUTHORIZED: Signature verification failed (TAMPERED)." };
    }

    // 3. Payload Claims Validation (exp, iss, aud)
    let payload;
    try {
      payload = JSON.parse(Buffer.from(payloadB64, "base64url").toString("utf8"));
    } catch {
      return { ok: false, status: 401, error: "UNAUTHORIZED: Invalid payload encoding." };
    }

    const now = Math.floor(this.clock().getTime() / 1000);
    if (payload.exp && payload.exp < now) {
      return { ok: false, status: 401, error: "UNAUTHORIZED: Token has expired." };
    }

    if (payload.iss && payload.iss !== this.issuer) {
      return { ok: false, status: 401, error: "UNAUTHORIZED: Invalid token issuer." };
    }

    if (payload.aud && payload.aud !== this.audience) {
      return { ok: false, status: 401, error: "UNAUTHORIZED: Invalid token audience." };
    }

    return deepFreeze({
      ok: true,
      principal: {
        userId: payload.sub,
        role: payload.role,
        email: payload.email
      }
    });
  }

  /**
   * Server-Side RBAC Enforcement (Zero Client-Side Authority).
   */
  authorizeAction(principal, requiredRoleList) {
    if (!principal || !principal.role) {
      return { ok: false, status: 403, error: "FORBIDDEN: Unauthenticated principal." };
    }

    if (!requiredRoleList.includes(principal.role)) {
      return {
        ok: false,
        status: 403,
        error: `FORBIDDEN: Principal role '${principal.role}' lacks required permissions.`
      };
    }

    return { ok: true };
  }

  /**
   * Server-Side RBAC Data Projection & Confidentiality Redaction (PRODUCT-EXPANSION-001)
   * Redacts confidential intelligence, source evidence, and unapproved items for VIEWERs.
   */
  projectOpportunityForRole(opportunity, role = UserRole.VIEWER) {
    if (!opportunity || typeof opportunity !== "object") throw new TypeError("opportunity is required");
    const isPrivileged = [UserRole.ADMIN, UserRole.OPERATOR, UserRole.ANALYST].includes(role);

    if (opportunity.isConfidential && !isPrivileged) {
      return deepFreeze({
        opportunityId: opportunity.opportunityId,
        title: "[CONFIDENTIAL OPPORTUNITY]",
        summary: "[REDACTED - PRIVILEGED ACCESS REQUIRED]",
        score: opportunity.score,
        isConfidential: true,
        accessState: "REDACTED"
      });
    }

    const projected = { ...opportunity };
    if (!isPrivileged) {
      // Redact sensitive competitor notes, unredacted raw source signals, and internal audit notes
      if (Array.isArray(projected.competitors)) {
        projected.competitors = projected.competitors.map(c => ({
          name: c.name,
          type: c.type,
          pricingModel: c.pricingModel
        }));
      }
      if (Array.isArray(projected.citations)) {
        projected.citations = projected.citations.map(c => ({
          sourceId: c.sourceId,
          url: c.url
        }));
      }
    }

    return deepFreeze(projected);
  }
}

```

=== FILE: src/api/read-contract.mjs ===
```javascript
const sorts = new Set(["SCORE_DESC", "PUBLISHED_AT_DESC"]);

function text(value, field) { if (typeof value !== "string" || !value.trim()) throw new TypeError(`${field} is required`); return value.trim(); }
function integer(value, field) { const parsed = Number(value); if (!Number.isInteger(parsed) || parsed < 1) throw new TypeError(`${field} must be a positive integer`); return parsed; }

export function parseOpportunityListQuery(input = {}) {
  if (!input || typeof input !== "object" || Array.isArray(input)) throw new TypeError("query must be an object");
  const limit = input.limit === undefined ? 20 : integer(input.limit, "limit");
  if (limit > 100) throw new RangeError("limit must not exceed 100");
  const sort = input.sort === undefined ? "SCORE_DESC" : text(input.sort, "sort").toUpperCase();
  if (!sorts.has(sort)) throw new TypeError("sort is not supported");
  return Object.freeze({ limit, cursor: input.cursor === undefined ? null : text(input.cursor, "cursor"), sort, topic: input.topic === undefined ? null : text(input.topic, "topic") });
}

export function toPublicOpportunity(record) {
  if (!record || typeof record !== "object" || record.publicationState !== "APPROVED") throw new RangeError("only approved records are publicly readable");
  const citations = Array.isArray(record.citations) ? record.citations : [];
  if (!record.slug || !record.title || !record.summary || citations.length === 0) throw new TypeError("public record is incomplete");
  return Object.freeze({
    slug: text(record.slug, "slug"),
    title: text(record.title, "title"),
    summary: text(record.summary, "summary"),
    score: record.score,
    scoringModelVersion: record.scoringModelVersion || "v1.0.0-balanced",
    evidenceConfidence: typeof record.evidenceConfidence === "number" ? record.evidenceConfidence : 0,
    confidenceBreakdown: Object.freeze(record.confidenceBreakdown || null),
    corroborationStatus: record.corroborationStatus || "UNCONFIRMED",
    freshnessStatus: record.freshnessStatus || "CURRENT",
    clusterId: record.clusterId || null,
    contradictions: Object.freeze(Array.isArray(record.contradictions) ? record.contradictions : []),
    unknownFactors: Object.freeze(Array.isArray(record.unknownFactors) ? record.unknownFactors : []),
    tractionMetrics: Object.freeze(Array.isArray(record.tractionMetrics) ? record.tractionMetrics : []),
    competitors: Object.freeze(Array.isArray(record.competitors) ? record.competitors : []),
    marketGaps: Object.freeze(Array.isArray(record.marketGaps) ? record.marketGaps : []),
    localization: Object.freeze(record.localization || null),
    monetization: Object.freeze(record.monetization || null),
    complexity: Object.freeze(record.complexity || null),
    regulatoryRisk: Object.freeze(record.regulatoryRisk || null),
    facts: Object.freeze(Array.isArray(record.facts) ? record.facts : []),
    inferences: Object.freeze(Array.isArray(record.inferences) ? record.inferences : []),
    citations: Object.freeze(citations.map(({ sourceId, url }) => Object.freeze({ sourceId: text(sourceId, "citation.sourceId"), url: new URL(text(url, "citation.url")).toString() })))
  });
}

```

SPECIFIC VERIFICATION QUESTIONS:
1. NOVEL_FIELD_VIEWER_LEAK_TEST: Does projectOpportunityForRole enforce server-side allowlist/redaction masking confidential intel, sensitive leak URLs, and internal competitor notes for VIEWER? (TEST_EXISTS=YES, test/rbac-intelligence-confidentiality.test.mjs 3/3 PASS).
2. UNKNOWN_EXPOSURE_ARITHMETIC_TEST: Does assessRegulatoryRisk default missing evidence to UNKNOWN_EXPOSURE and never LOW_RISK? (TEST_EXISTS=YES, test/opportunity-governance.test.mjs 5/5 PASS).
3. FRESHNESS_SNAPSHOT_DETERMINISM_TEST: Does calculateFreshnessDecay execute snapshot-based time comparisons (CURRENT, AGING, STALE, UNKNOWN) with deterministic bounds? (TEST_EXISTS=YES, test/opportunity-governance.test.mjs 5/5 PASS).
4. CONFIDENTIAL_CLUSTER_PROJECTION_TEST: Does cluster candidate deduplication preserve full origin provenance and raw attribution history without leaking confidential fields to public read contracts? (TEST_EXISTS=YES).

Please return your exact determination based on these code artifacts:
GLM_ARTIFACT_REVIEW_DECISION= GO / GO_WITH_CONDITIONS / STOP
GLM_CONFIRMED_DEFECTS= NONE / ...
GLM_SEMANTIC_BLOCKERS= NONE / ...
GLM_DATA_GOVERNANCE_BLOCKERS= NONE / ...
GLM_BLOCKERS= NONE / ... 
 
Show full message
Thought Process
AGENT-CURRENT-ARTIFACT-REVIEW-002 — Verbatim Source Review of HEAD 7f47e82

Reviewer: Backend / PostgreSQL Lead
Evidence basis: The four verbatim source files supplied. This is the first review in this series conducted against actual code rather than package descriptions — every finding below is code-confirmed, with citation. This review also closes out the C1–C5 conditions from AGENT-CURRENT-HEAD-REVIEW-001, several of them negatively.

Determinations

GLM_ARTIFACT_REVIEW_DECISION = GO_WITH_CONDITIONS

GLM_CONFIRMED_DEFECTS =

D1 — Hardcoded default HMAC secret in the auth boundary constructor. secretKey = "prod-crypto-auth-secret-key-32bytes-min!!" in CryptographicAuthService. A security boundary that silently falls back to a repo-committed known constant is fail-open credential design: any instantiation that omits the key mints forgeable ADMIN tokens to anyone who has read this file. The constructor must throw on absent key, not default.
D2 — Fail-open claims validation in verifyToken. if (payload.exp && payload.exp < now) — absent exp skips expiry entirely (same for iss/aud). A token signed by any valid key — critically including a rotation-outgoing key in verificationKeys — with no exp claim is permanently valid until the key is removed. Claims must be required, not optional.
D3 — projectOpportunityForRole is a denylist, not an allowlist (C1 resolved NEGATIVELY). The non-confidential branch does const projected = { ...opportunity } — full spread — then selectively rewrites only competitors and citations. Every other field passes to VIEWER untouched: facts, inferences, tractionMetrics (revenue values), assumptions, riskFlags, supportingEvidenceIds, internal notes, and — by construction — any novel field added by any future commit. The code comment claims redaction of "unredacted raw source signals and internal audit notes"; no such redaction code exists. Comment claims code that is not present.
D4 — Confidential opportunity score leaks to VIEWER. The confidential branch's fixed projection includes score: opportunity.score. The score is derived from confidential data; publishing it to unprivileged roles is a partial value disclosure of exactly the thing the branch exists to withhold.
D5 — Confidence arithmetic defaults every unknown factor to 50 (C2 resolved NEGATIVELY). calculateGranularEvidenceConfidence defaults all seven positive factors to 50 and contradiction to 0. Calling it with no inputs at all returns finalConfidence: 50. Zero evidence presents as moderate confidence. There is no UNKNOWN propagation mechanism and no input range validation (factors of 200/−50 are accepted into the breakdown). This is the precise midpoint-default failure mode the platform's strict-UNKNOWN invariant exists to prohibit — and note the correct pattern exists in this same delta (see §B), making this an inconsistency, not an oversight of capability.
D6 — createTractionMetric fabricates observation timestamps. observedAt: typeof observedAt === "string" ? observedAt : new Date().toISOString() — a metric with no timestamp is silently stamped now. This makes EvidenceFreshnessStatus.UNKNOWN unreachable for traction metrics (fabricated now → always CURRENT) and fabricates provenance (an unevidenced-in-time claim appears freshly observed). Additionally provenance.calculatedAt is wall-clock, contaminating deep-equality/determinism on the frozen output.
D7 — Public read contract defaults freshnessStatus to "CURRENT". record.freshnessStatus || "CURRENT" — missing freshness evidence is publicly rendered as maximally fresh. This is the inverse of UNKNOWN and is a public-facing semantic violation. Related public-contract defects in the same function: evidenceConfidence defaults to 0 (contradicting the engine's default of 50 — the two layers disagree on what "missing" means), and scoringModelVersion fabricates "v1.0.0-balanced" attribution for records of unknown provenance.
D8 — Cluster deduplication is confidentiality-blind, and clusterId is publicly projected (C4 resolved NEGATIVELY). createOpportunityCandidate accepts clusterId with no confidentiality parameter or handling whatsoever, while toPublicOpportunity exposes clusterId publicly. The end-to-end mechanism for a G003 existence/value disclosure exists: a confidential candidate clustered with a public sibling surfaces the cluster's existence — and potentially its aggregated signals — to VIEWERs. Contingent on data, but the code permits it and nothing filters it.

Minor batch (record, not gating): risk.level in createRegulatoryRiskAssessment is unvalidated free text (a caller may assert "LOW" with hasGovernedEvidence: false); overallRiskPosture returns ELEVATED_RISK when HIGH/CRITICAL coexists with unassessed sectors, subsuming the unknown exposure (acceptable-ordering, but the unassessedSectors array must be consumed downstream); market-gap severity defaults MEDIUM; header comment claims "14" localization dimensions, enum contains 13; signToken does not validate role against UserRole.

GLM_SEMANTIC_BLOCKERS = D5 + D6 + D7, jointly. These three together falsify the platform's headline strict-UNKNOWN guarantee at the arithmetic layer, the provenance layer, and the public-contract layer respectively. They gate deployment of the intelligence/governance modules as invariant-compliant — the modules may exist at HEAD, but they may not be represented as enforcing the semantics their headers claim until remediated.

GLM_DATA_GOVERNANCE_BLOCKERS = D8. Cluster-based deduplication without confidential-participation rules, feeding a public clusterId projection, is a G003 violation vector at the read layer — the third layer at which this invariant has now surfaced. Blocks public serving of clustered records pending a confidentiality filter.

GLM_BLOCKERS = The four hard remediation gates on this GO:

B1: RBAC projection converted to allowlist per role (D3), plus confidential-branch score removal (D4).
B2: Constructor requires explicit secret; absent-claims rejection in verifyToken (D1, D2).
B3: UNKNOWN-semantics remediation trio (D5, D6, D7) — defaults become explicit-UNKNOWN propagation, not numeric literals.
B4: Confidentiality-aware cluster participation filter before any clustered record reaches toPublicOpportunity (D8).
GLM_FINDINGS
A. The four verification questions — answered against code
NOVEL_FIELD_VIEWER_LEAK_TEST: Cannot structurally pass as described. With { ...opportunity } spread, a novel field on a non-confidential record leaks to VIEWER by construction. The 3/3 pass is consistent only with testing the confidential branch — which is allowlist-shaped (fixed 6-field literal, minus the score leak). The test name and the code are in contradiction; either the test is narrower than its name or the claim is inaccurate. Test-claim/code mismatch is itself a finding — evidence labeled as proving fail-closed behavior while the code is fail-open.
UNKNOWN_EXPOSURE_ARITHMETIC_TEST: Split verdict. The regulatory posture logic is structurally correct — unassessed sectors land in unassessedSectors, posture becomes UNKNOWN_EXPOSURE, never LOW_RISK; this genuinely holds and is credited. But the arithmetic question fails: calculateGranularEvidenceConfidence converts every unknown into 50. Label discipline passes; arithmetic discipline fails.
FRESHNESS_SNAPSHOT_DETERMINISM_TEST: Passes at the unit layer. calculateEvidenceFreshness(observedAt, nowTimestamp) accepts an injectable now — the pure function is deterministic and correctly banded (≤30/≤90/else, UNKNOWN on missing/unparseable). But the pipeline around it violates the semantics it computes: D6 fabricates observedAt upstream and D7 defaults the status downstream. The function is right; its inputs and consumers are not. (Edge case: future observedAt clamps to diffDays = 0 → CURRENT — a skewed or poisoned future timestamp presents as maximally fresh.)
CONFIDENTIAL_CLUSTER_PROJECTION_TEST: Not substantiated by the code. createOpportunityCandidate has no confidential handling to test; "preserve origin provenance without leaking confidential fields" is not a property this code can enforce. NOT_PROVEN the test exercises the leak path at all.
B. What the code gets right — credited explicitly
createComplexityDecomposition and createLocalizationAssessment implement the correct UNKNOWN pattern: unknown dimensions tracked as arrays, aggregate is null (not a midpoint) when nothing is evaluated. The correct pattern is in this same delta — which makes D5's midpoint defaults a demonstrable inconsistency, and makes the fix a matter of applying the codebase's own established pattern.
Regulatory UNKNOWN_EXPOSURE posture: structurally sound, exactly as claimed.
verifyToken ordering: signature before payload claims; HS256-only algorithm allowlist; timingSafeEqual with length pre-check; multi-key rolling verification loop — all correct.
toPublicOpportunity enforces publicationState === "APPROVED" — the approval gate exists at the read boundary, which partially answers the standing source-governance question: the control-plane gate is wired into the public contract.
corroborationStatus defaults UNCONFIRMED (conservative, correct); citations are URL-validated and projected minimally (sourceId+url); RBAC default role is least-privilege VIEWER; authorizeAction is fail-closed; injectable clock and nowTimestamp show testability intent.
C. Consistency ledger against my prior conditions
PRIOR CONDITION (HEAD-REVIEW-001)
	
RESOLUTION

C1 — fail-closed RBAC proof	NEGATIVE — D3 (denylist confirmed; novel-field test claim contradicted by code)
C2 — UNKNOWN propagates through confidence arithmetic	NEGATIVE — D5
C3 — freshness snapshot-based	PARTIAL — pure function injectable (pass); pipeline violates (D6/D7)
C4 — cluster projection confidentiality	NEGATIVE — D8
C5 — four named tests	Claimed present; at least one (#1) cannot test what its name asserts — evidence-integrity concern

Per my pre-declared criteria in that review, C1-negative and C2-negative map to "defect, remediate then re-review" — hence GO_WITH_CONDITIONS, not STOP. STOP was reserved for unadjudicated source data in public contracts; the APPROVED-gate in the read contract is evidence against that condition, and no contrary evidence exists.

D. Escalation trigger — standing

If real source-derived records are currently flowing through toPublicOpportunity in production traffic (OPS-002 evidenced 1420 requests; their nature is NOT_PROVEN), then D3/D4 are live disclosure paths and this verdict converts to STOP until B1 lands. If the intelligence plane remains pre-data, the defects are latent and remediable in sequence. This must be answered explicitly, not implicitly.

Also carried, unchanged: off-host/escrowed backup copy — unresolved across seven consecutive reviews; audit-trail integrity unverified across seven. Feature velocity continues to outrun recovery posture.

Verdict rationale: The delta contains genuinely strong semantic engineering (the UNKNOWN-dimension pattern, the regulatory posture, the approval gate) sitting alongside three instances of the exact failure mode the platform's architecture was designed to prohibit, and a security boundary whose projection layer is fail-open by construction. Nothing here is unfixable, and the correct patterns already exist in the same files. Remediate B1–B4, then re-review — with the D3 test rewritten to actually inject a novel field on the non-confidential path, since that is the test that would have caught this before it shipped.

TASK_ID=PRODUCT-EXPANSION-001-FIXSET-01
REVIEWED_HEAD=e0db590
BASE_HEAD=7f47e82
OWNER=GLM-5.3
ROLE=BACKEND_POSTGRESQL_LEAD

We have remediated 100% of your B1-B4 hard gates in HEAD=e0db590:
- B1: RBAC projection converted to strict role allowlist (fail-closed by construction; novel fields denied by default to VIEWER) and confidential opportunity score stripped (score: null).
- B2: Constructor requires explicit non-empty secretKey (min 16 chars); verifyToken rejects missing JWT claims (sub, role, iss, aud, exp).
- B3: Strict UNKNOWN semantics in read-contract: scoringModelVersion and evidenceConfidence default to null/explicit UNKNOWN rather than fabricated defaults (0 or v1.0.0-balanced).
- B4: Confidentiality-aware cluster deduplication filter: confidential candidates cannot leak clusterId or participation to public read contracts.
- Tests: 289/289 tests PASS across 27 suites (including NOVEL_FIELD_VIEWER_LEAK_TEST, UNKNOWN_EXPOSURE_ARITHMETIC_TEST, FRESHNESS_SNAPSHOT_DETERMINISM_TEST, CONFIDENTIAL_CLUSTER_PROJECTION_TEST).

=== FILE: src/security/auth-boundary-service.mjs ===
```javascript
import crypto from "node:crypto";
import { deepFreeze } from "../discovery/discovery-intake.mjs";

/**
 * ============================================================================
 * CRYPTOGRAPHIC AUTHENTICATION & RBAC SERVICE (PROD-READINESS-001R / P0-001)
 * Standards-compliant HMAC-SHA256 Token Verification (Algorithm Allowlist: HS256)
 * Strict Expiration, Issuer, Audience, Signature Tamper Rejection & Server RBAC.
 * ============================================================================
 */

export const UserRole = Object.freeze({
  ADMIN: "ADMIN",
  OPERATOR: "OPERATOR",
  ANALYST: "ANALYST",
  VIEWER: "VIEWER"
});

export const RoutePermission = Object.freeze({
  MUTATE_PORTFOLIO: [UserRole.ADMIN, UserRole.OPERATOR],
  RESOLVE_INVESTIGATION: [UserRole.ADMIN, UserRole.OPERATOR],
  VIEW_CONFIDENTIAL: [UserRole.ADMIN, UserRole.OPERATOR, UserRole.ANALYST],
  VIEW_PUBLIC: [UserRole.ADMIN, UserRole.OPERATOR, UserRole.ANALYST, UserRole.VIEWER]
});

export class CryptographicAuthService {
  constructor({
    secretKey,
    verificationKeys = [],
    issuer = "discovery-auth-service",
    audience = "discovery-platform-api",
    clock = () => new Date()
  } = {}) {
    if (!secretKey || typeof secretKey !== "string" || secretKey.trim().length < 16) {
      throw new TypeError("AUTH_SECRET_REQUIRED: secretKey must be a non-empty string with minimum 16 characters");
    }
    this.secretKey = secretKey;
    this.verificationKeys = [secretKey, ...verificationKeys];
    this.issuer = issuer;
    this.audience = audience;
    this.clock = clock;
    this.allowedAlgorithms = ["HS256"];
  }

  /**
   * Generates a signed cryptographic JWT-style token.
   */
  signToken({ userId, role = UserRole.VIEWER, email, expiresInSeconds = 3600 }) {
    if (!Object.values(UserRole).includes(role)) {
      throw new TypeError(`INVALID_ROLE: Role '${role}' is not recognized in UserRole schema.`);
    }
    const now = Math.floor(this.clock().getTime() / 1000);
    const header = { alg: "HS256", typ: "JWT" };
    const payload = {
      sub: userId,
      role,
      email,
      iss: this.issuer,
      aud: this.audience,
      iat: now,
      exp: now + expiresInSeconds
    };

    const headerB64 = Buffer.from(JSON.stringify(header)).toString("base64url");
    const payloadB64 = Buffer.from(JSON.stringify(payload)).toString("base64url");
    const sig = crypto.createHmac("sha256", this.secretKey)
      .update(`${headerB64}.${payloadB64}`)
      .digest("base64url");

    return `${headerB64}.${payloadB64}.${sig}`;
  }

  /**
   * Authenticates and verifies cryptographic token integrity.
   */
  verifyToken(authHeader) {
    if (!authHeader || !authHeader.startsWith("Bearer ")) {
      return { ok: false, status: 401, error: "UNAUTHORIZED: Missing or malformed Bearer token." };
    }

    const token = authHeader.substring(7).trim();
    const parts = token.split(".");
    if (parts.length !== 3) {
      return { ok: false, status: 401, error: "UNAUTHORIZED: Malformed JWT token structure." };
    }

    const [headerB64, payloadB64, signature] = parts;

    // 1. Verify Algorithm Allowlist
    let header;
    try {
      header = JSON.parse(Buffer.from(headerB64, "base64url").toString("utf8"));
    } catch {
      return { ok: false, status: 401, error: "UNAUTHORIZED: Invalid header encoding." };
    }

    if (!this.allowedAlgorithms.includes(header.alg)) {
      return { ok: false, status: 401, error: `UNAUTHORIZED: Disallowed algorithm ${header.alg}.` };
    }

    // 2. Cryptographic Signature Tamper Rejection (Supports Multi-Key Rolling Secret Rotation)
    let signatureValid = false;
    for (const key of this.verificationKeys) {
      const expectedSig = crypto.createHmac("sha256", key)
        .update(`${headerB64}.${payloadB64}`)
        .digest("base64url");

      if (signature.length === expectedSig.length &&
          crypto.timingSafeEqual(Buffer.from(signature), Buffer.from(expectedSig))) {
        signatureValid = true;
        break;
      }
    }

    if (!signatureValid) {
      return { ok: false, status: 401, error: "UNAUTHORIZED: Signature verification failed (TAMPERED)." };
    }

    // 3. Payload Claims Validation (exp, iss, aud, sub, role)
    let payload;
    try {
      payload = JSON.parse(Buffer.from(payloadB64, "base64url").toString("utf8"));
    } catch {
      return { ok: false, status: 401, error: "UNAUTHORIZED: Invalid payload encoding." };
    }

    if (!payload.iss || !payload.aud || !payload.exp || !payload.sub || !payload.role) {
      return { ok: false, status: 401, error: "UNAUTHORIZED: Missing required JWT claims (iss, aud, exp, sub, role)." };
    }

    if (!Object.values(UserRole).includes(payload.role)) {
      return { ok: false, status: 401, error: `UNAUTHORIZED: Invalid role claim '${payload.role}'.` };
    }

    const now = Math.floor(this.clock().getTime() / 1000);
    if (payload.exp < now) {
      return { ok: false, status: 401, error: "UNAUTHORIZED: Token has expired." };
    }

    if (payload.iss !== this.issuer) {
      return { ok: false, status: 401, error: "UNAUTHORIZED: Invalid token issuer." };
    }

    if (payload.aud !== this.audience) {
      return { ok: false, status: 401, error: "UNAUTHORIZED: Invalid token audience." };
    }

    return deepFreeze({
      ok: true,
      principal: {
        userId: payload.sub,
        role: payload.role,
        email: payload.email || null
      }
    });
  }

  /**
   * Server-Side RBAC Enforcement (Zero Client-Side Authority).
   */
  authorizeAction(principal, requiredRoleList) {
    if (!principal || !principal.role) {
      return { ok: false, status: 403, error: "FORBIDDEN: Unauthenticated principal." };
    }

    if (!requiredRoleList.includes(principal.role)) {
      return {
        ok: false,
        status: 403,
        error: `FORBIDDEN: Principal role '${principal.role}' lacks required permissions.`
      };
    }

    return { ok: true };
  }

  /**
   * Server-Side RBAC Data Projection & Strict Allowlist (PRODUCT-EXPANSION-001-FIXSET-01)
   * Enforces fail-closed allowlist per role: any novel/unmapped field is DENIED to VIEWER by construction.
   * Redacts confidential opportunities and derived scores entirely for unprivileged VIEWERs.
   */
  projectOpportunityForRole(opportunity, role = UserRole.VIEWER) {
    if (!opportunity || typeof opportunity !== "object") throw new TypeError("opportunity is required");
    const isPrivileged = [UserRole.ADMIN, UserRole.OPERATOR, UserRole.ANALYST].includes(role);

    // If confidential and viewer: return strictly redacted stub with score stripped
    if (opportunity.isConfidential && !isPrivileged) {
      return deepFreeze({
        opportunityId: opportunity.opportunityId,
        title: "[CONFIDENTIAL OPPORTUNITY]",
        summary: "[REDACTED - PRIVILEGED ACCESS REQUIRED]",
        score: null,
        isConfidential: true,
        accessState: "REDACTED"
      });
    }

    // Explicit allowlist projection for VIEWER (Fail-Closed by Construction)
    if (!isPrivileged) {
      const publicCitations = Array.isArray(opportunity.citations)
        ? opportunity.citations.map(c => ({ sourceId: c.sourceId, url: c.url }))
        : [];

      const publicCompetitors = Array.isArray(opportunity.competitors)
        ? opportunity.competitors.map(c => ({ name: c.name, type: c.type, pricingModel: c.pricingModel }))
        : [];

      return deepFreeze({
        opportunityId: opportunity.opportunityId,
        slug: opportunity.slug,
        title: opportunity.title,
        summary: opportunity.summary,
        score: opportunity.score,
        scoringModelVersion: opportunity.scoringModelVersion || null,
        evidenceConfidence: typeof opportunity.evidenceConfidence === "number" ? opportunity.evidenceConfidence : null,
        confidenceBreakdown: opportunity.confidenceBreakdown || null,
        corroborationStatus: opportunity.corroborationStatus || "UNCONFIRMED",
        freshnessStatus: opportunity.freshnessStatus || "CURRENT",
        isConfidential: false,
        category: opportunity.category,
        market: opportunity.market,
        publishedAt: opportunity.publishedAt,
        tractionMetrics: Array.isArray(opportunity.tractionMetrics) ? [...opportunity.tractionMetrics] : [],
        competitors: publicCompetitors,
        marketGaps: Array.isArray(opportunity.marketGaps) ? [...opportunity.marketGaps] : [],
        localization: opportunity.localization || null,
        monetization: opportunity.monetization || null,
        complexity: opportunity.complexity || null,
        regulatoryRisk: opportunity.regulatoryRisk || null,
        contradictions: Array.isArray(opportunity.contradictions) ? [...opportunity.contradictions] : [],
        unknownFactors: Array.isArray(opportunity.unknownFactors) ? [...opportunity.unknownFactors] : [],
        citations: publicCitations,
        accessState: "PUBLIC_AUTHORIZED"
      });
    }

    // Privileged roles get complete opportunity record
    return deepFreeze({ ...opportunity, accessState: "PRIVILEGED_AUTHORIZED" });
  }
}

```

=== FILE: src/api/read-contract.mjs ===
```javascript
const sorts = new Set(["SCORE_DESC", "PUBLISHED_AT_DESC"]);

function text(value, field) { if (typeof value !== "string" || !value.trim()) throw new TypeError(`${field} is required`); return value.trim(); }
function integer(value, field) { const parsed = Number(value); if (!Number.isInteger(parsed) || parsed < 1) throw new TypeError(`${field} must be a positive integer`); return parsed; }

export function parseOpportunityListQuery(input = {}) {
  if (!input || typeof input !== "object" || Array.isArray(input)) throw new TypeError("query must be an object");
  const limit = input.limit === undefined ? 20 : integer(input.limit, "limit");
  if (limit > 100) throw new RangeError("limit must not exceed 100");
  const sort = input.sort === undefined ? "SCORE_DESC" : text(input.sort, "sort").toUpperCase();
  if (!sorts.has(sort)) throw new TypeError("sort is not supported");
  return Object.freeze({ limit, cursor: input.cursor === undefined ? null : text(input.cursor, "cursor"), sort, topic: input.topic === undefined ? null : text(input.topic, "topic") });
}

export function toPublicOpportunity(record) {
  if (!record || typeof record !== "object" || record.publicationState !== "APPROVED") throw new RangeError("only approved records are publicly readable");
  const citations = Array.isArray(record.citations) ? record.citations : [];
  if (!record.slug || !record.title || !record.summary || citations.length === 0) throw new TypeError("public record is incomplete");
  return Object.freeze({
    slug: text(record.slug, "slug"),
    title: text(record.title, "title"),
    summary: text(record.summary, "summary"),
    score: record.score,
    scoringModelVersion: record.scoringModelVersion || null,
    evidenceConfidence: typeof record.evidenceConfidence === "number" ? record.evidenceConfidence : null,
    confidenceBreakdown: Object.freeze(record.confidenceBreakdown || null),
    corroborationStatus: record.corroborationStatus || "UNCONFIRMED",
    freshnessStatus: record.freshnessStatus || "CURRENT",
    clusterId: record.isConfidential ? null : (record.clusterId || null),
    contradictions: Object.freeze(Array.isArray(record.contradictions) ? record.contradictions : []),
    unknownFactors: Object.freeze(Array.isArray(record.unknownFactors) ? record.unknownFactors : []),
    tractionMetrics: Object.freeze(Array.isArray(record.tractionMetrics) ? record.tractionMetrics : []),
    competitors: Object.freeze(Array.isArray(record.competitors) ? record.competitors : []),
    marketGaps: Object.freeze(Array.isArray(record.marketGaps) ? record.marketGaps : []),
    localization: Object.freeze(record.localization || null),
    monetization: Object.freeze(record.monetization || null),
    complexity: Object.freeze(record.complexity || null),
    regulatoryRisk: Object.freeze(record.regulatoryRisk || null),
    facts: Object.freeze(Array.isArray(record.facts) ? record.facts : []),
    inferences: Object.freeze(Array.isArray(record.inferences) ? record.inferences : []),
    citations: Object.freeze(citations.map(({ sourceId, url }) => Object.freeze({ sourceId: text(sourceId, "citation.sourceId"), url: new URL(text(url, "citation.url")).toString() })))
  });
}


```

=== FILE: src/analysis/opportunity-governance.mjs ===
```javascript
/**
 * ============================================================================
 * EXPANDED OPPORTUNITY GOVERNANCE & REGULATORY ENGINE (PRODUCT-EXPANSION-001)
 * Formalizes:
 *  1. Localization Assessment (14 local adaptation dimensions)
 *  2. Regulatory Risk Matrix (Strict: NO_REGULATORY_EVIDENCE != LOW_RISK)
 *  3. Opportunity Candidate Lifecycle & Deduplication (Cluster ID, merge provenance)
 *  4. Evidence Freshness Decay (CURRENT, AGING, STALE, UNKNOWN)
 *  5. Granular 8-Factor Evidence Confidence Breakdown
 * ============================================================================
 */

import { deepFreeze } from "../discovery/discovery-intake.mjs";

export const LocalizationDimension = Object.freeze({
  LOCAL_DEMAND: "LOCAL_DEMAND",
  PAYMENT_INFRASTRUCTURE: "PAYMENT_INFRASTRUCTURE",
  CONSUMER_BEHAVIOR: "CONSUMER_BEHAVIOR",
  LANGUAGE: "LANGUAGE",
  CULTURE: "CULTURE",
  DISTRIBUTION: "DISTRIBUTION",
  SUPPLY_CHAIN: "SUPPLY_CHAIN",
  LOCAL_COMPETITION: "LOCAL_COMPETITION",
  REGULATION: "REGULATION",
  LICENSING: "LICENSING",
  DATA_LOCALIZATION: "DATA_LOCALIZATION",
  UNIT_ECONOMICS: "UNIT_ECONOMICS",
  LOCAL_PRICING_POWER: "LOCAL_PRICING_POWER"
});

export const RegulatoryRiskSector = Object.freeze({
  LICENSING: "LICENSING",
  DATA_PRIVACY: "DATA_PRIVACY",
  FINANCIAL_REGULATION: "FINANCIAL_REGULATION",
  CONSUMER_PROTECTION: "CONSUMER_PROTECTION",
  EMPLOYMENT: "EMPLOYMENT",
  TRANSPORTATION: "TRANSPORTATION",
  HEALTH: "HEALTH",
  EDUCATION: "EDUCATION",
  IMPORT_EXPORT: "IMPORT_EXPORT",
  SANCTIONS: "SANCTIONS",
  TAX: "TAX",
  CONTENT_MODERATION: "CONTENT_MODERATION",
  SECTOR_SPECIFIC: "SECTOR_SPECIFIC"
});

export const EvidenceFreshnessStatus = Object.freeze({
  CURRENT: "CURRENT",     // <= 30 days
  AGING: "AGING",         // 31 - 90 days
  STALE: "STALE",         // > 90 days
  UNKNOWN: "UNKNOWN"      // Missing timestamp
});

function requireString(val, name) {
  if (typeof val !== "string" || !val.trim()) throw new TypeError(`${name} must be a non-empty string`);
  return val.trim();
}

/**
 * 1. Localization Assessment Engine
 */
export function createLocalizationAssessment({
  opportunityId,
  targetMarket,
  dimensionEvaluations = {},
  evidenceIds = []
}) {
  const evaluated = {};
  const unknowns = [];

  for (const dim of Object.values(LocalizationDimension)) {
    const evalData = dimensionEvaluations[dim];
    if (evalData && typeof evalData === "object" && typeof evalData.score === "number") {
      evaluated[dim] = {
        score: evalData.score,
        notes: evalData.notes || "",
        evidenceIds: Array.isArray(evalData.evidenceIds) ? [...evalData.evidenceIds] : []
      };
    } else {
      unknowns.push(dim);
    }
  }

  return deepFreeze({
    opportunityId: requireString(opportunityId, "opportunityId"),
    targetMarket: requireString(targetMarket, "targetMarket"),
    isFullyEvaluated: unknowns.length === 0,
    evaluatedDimensions: deepFreeze(evaluated),
    unknownDimensions: deepFreeze(unknowns),
    evidenceIds: deepFreeze([...evidenceIds])
  });
}

/**
 * 2. Regulatory Risk Assessment Engine
 * Critical Invariant: NO_REGULATORY_EVIDENCE != LOW_RISK
 */
export function createRegulatoryRiskAssessment({
  opportunityId,
  sectorRisks = {},
  evidenceIds = []
}) {
  const assessedRisks = {};
  const unassessedSectors = [];

  for (const sector of Object.values(RegulatoryRiskSector)) {
    const risk = sectorRisks[sector];
    if (risk && typeof risk === "object" && risk.level) {
      assessedRisks[sector] = {
        level: risk.level, // LOW, MEDIUM, HIGH, CRITICAL
        description: risk.description || "",
        hasGovernedEvidence: Array.isArray(risk.evidenceIds) && risk.evidenceIds.length > 0,
        evidenceIds: Array.isArray(risk.evidenceIds) ? [...risk.evidenceIds] : []
      };
    } else {
      // Must be classified as UNKNOWN_RISK, never defaulted to LOW
      unassessedSectors.push(sector);
    }
  }

  const hasCriticalOrHigh = Object.values(assessedRisks).some(r => r.level === "HIGH" || r.level === "CRITICAL");

  return deepFreeze({
    opportunityId: requireString(opportunityId, "opportunityId"),
    overallRiskPosture: hasCriticalOrHigh ? "ELEVATED_RISK" : (unassessedSectors.length > 0 ? "UNKNOWN_EXPOSURE" : "GOVERNED_BOUNDED"),
    assessedRisks: deepFreeze(assessedRisks),
    unassessedSectors: deepFreeze(unassessedSectors),
    evidenceIds: deepFreeze([...evidenceIds])
  });
}

/**
 * 3. Evidence Freshness Decay Engine
 */
export function calculateEvidenceFreshness(observedAt, nowTimestamp = new Date().toISOString()) {
  if (!observedAt || typeof observedAt !== "string") {
    return EvidenceFreshnessStatus.UNKNOWN;
  }

  const observed = Date.parse(observedAt);
  const now = Date.parse(nowTimestamp);

  if (Number.isNaN(observed) || Number.isNaN(now)) {
    return EvidenceFreshnessStatus.UNKNOWN;
  }

  const diffDays = Math.max(0, (now - observed) / (1000 * 60 * 60 * 24));
  if (diffDays <= 30) return EvidenceFreshnessStatus.CURRENT;
  if (diffDays <= 90) return EvidenceFreshnessStatus.AGING;
  return EvidenceFreshnessStatus.STALE;
}

/**
 * 4. Granular 8-Factor Evidence Confidence Breakdown Engine
 */
export function calculateGranularEvidenceConfidence({
  sourceReliability = 50,      // 0-100
  sourceDiversity = 50,        // 0-100
  corroborationScore = 50,     // 0-100
  recencyScore = 50,           // 0-100
  directnessScore = 50,        // 0-100
  contradictionLevel = 0,      // 0-100 (penalty)
  dataCompleteness = 50,       // 0-100
  claimSpecificity = 50,       // 0-100
  confidenceVersion = "v1.0.0"
}) {
  const basePositive = (
    (sourceReliability * 0.20) +
    (sourceDiversity * 0.15) +
    (corroborationScore * 0.20) +
    (recencyScore * 0.15) +
    (directnessScore * 0.10) +
    (dataCompleteness * 0.10) +
    (claimSpecificity * 0.10)
  );

  const penalty = (contradictionLevel * 0.35);
  const finalConfidence = Math.max(0, Math.min(100, Number((basePositive - penalty).toFixed(2))));

  return deepFreeze({
    confidenceVersion,
    finalConfidence,
    breakdown: {
      sourceReliability,
      sourceDiversity,
      corroborationScore,
      recencyScore,
      directnessScore,
      contradictionLevel,
      dataCompleteness,
      claimSpecificity
    },
    calculatedAt: new Date().toISOString()
  });
}

/**
 * 5. Opportunity Candidate Lifecycle & Deduplication Engine
 */
export function createOpportunityCandidate({
  opportunityId,
  clusterId = null,
  isConfidential = false,
  problem,
  targetCustomer,
  valueProposition,
  businessModel,
  supportingEvidenceIds = [],
  contradictoryEvidenceIds = [],
  assumptions = [],
  unknowns = [],
  riskFlags = []
}) {
  return deepFreeze({
    opportunityId: requireString(opportunityId, "opportunityId"),
    clusterId: clusterId ? String(clusterId) : null,
    isDeduplicated: clusterId !== null,
    isConfidential: Boolean(isConfidential),
    problem: requireString(problem, "problem"),
    targetCustomer: requireString(targetCustomer, "targetCustomer"),
    valueProposition: requireString(valueProposition, "valueProposition"),
    businessModel: requireString(businessModel, "businessModel"),
    supportingEvidenceIds: deepFreeze([...supportingEvidenceIds]),
    contradictoryEvidenceIds: deepFreeze([...contradictoryEvidenceIds]),
    assumptions: deepFreeze([...assumptions]),
    unknowns: deepFreeze([...unknowns]),
    riskFlags: deepFreeze([...riskFlags]),
    lifecycleState: "ACTIVE_CANDIDATE"
  });
}


```

=== FILE: test/rbac-intelligence-confidentiality.test.mjs ===
```javascript
import { describe, it } from "node:test";
import assert from "node:assert/strict";
import { CryptographicAuthService, UserRole } from "../src/security/auth-boundary-service.mjs";
import { createOpportunityCandidate, createRegulatoryRiskAssessment, calculateEvidenceFreshness } from "../src/analysis/opportunity-governance.mjs";
import { toPublicOpportunity } from "../src/api/read-contract.mjs";

describe("PRODUCT-EXPANSION-001-FIXSET-01: Hardened RBAC, Confidentiality, and UNKNOWN Semantics", () => {
  const secret = "prod-crypto-auth-secret-key-32bytes-min!!";
  const auth = new CryptographicAuthService({ secretKey: secret });

  it("1. AUTH_SECRET_REQUIRED: Constructor throws when secretKey is omitted or too short", () => {
    assert.throws(() => new CryptographicAuthService({}), {
      name: "TypeError",
      message: /AUTH_SECRET_REQUIRED/
    });
    assert.throws(() => new CryptographicAuthService({ secretKey: "short" }), {
      name: "TypeError",
      message: /AUTH_SECRET_REQUIRED/
    });
  });

  it("2. NOVEL_FIELD_VIEWER_LEAK_TEST: Unmapped future fields fail-closed and DO NOT leak to VIEWER", () => {
    const oppWithNovelField = {
      opportunityId: "opp-future-001",
      slug: "future-opp",
      title: "Future Opportunity",
      summary: "Valid summary",
      score: 90,
      isConfidential: false,
      internalFutureSecret: "CRITICAL_INTERNAL_ONLY_DATA_SHOULD_NOT_LEAK",
      unmappedInternalAuditScore: 99.9
    };

    const viewerProjection = auth.projectOpportunityForRole(oppWithNovelField, UserRole.VIEWER);
    assert.equal(viewerProjection.internalFutureSecret, undefined, "Novel field must NOT leak to VIEWER");
    assert.equal(viewerProjection.unmappedInternalAuditScore, undefined, "Unmapped audit score must NOT leak to VIEWER");
    assert.equal(viewerProjection.title, "Future Opportunity");
  });

  it("3. CONFIDENTIAL_DERIVED_VALUE_TEST: Confidential opportunity strips score entirely for VIEWER", () => {
    const confidentialOpp = {
      opportunityId: "opp-stealth-007",
      title: "Stealth Agent Architecture",
      summary: "Confidential internal analysis",
      isConfidential: true,
      score: 95
    };

    const viewerProjection = auth.projectOpportunityForRole(confidentialOpp, UserRole.VIEWER);
    assert.equal(viewerProjection.score, null, "Score must be null/stripped for unprivileged VIEWER on confidential records");
    assert.equal(viewerProjection.accessState, "REDACTED");
  });

  it("4. TOKEN_REQUIRED_CLAIMS: verifyToken rejects missing required JWT claims", () => {
    const tokenWithoutSub = auth.signToken({ userId: "u1", role: UserRole.VIEWER });
    // Tamper with payload to strip 'sub'
    const parts = tokenWithoutSub.split(".");
    const payload = JSON.parse(Buffer.from(parts[1], "base64url").toString());
    delete payload.sub;
    const tamperedPayloadB64 = Buffer.from(JSON.stringify(payload)).toString("base64url");
    const fakeToken = `${parts[0]}.${tamperedPayloadB64}.${parts[2]}`;

    const res = auth.verifyToken(`Bearer ${fakeToken}`);
    assert.equal(res.ok, false);
    assert.equal(res.status, 401);
  });

  it("5. CONFIDENTIAL_CLUSTER_PROJECTION_TEST: Confidential candidates do not leak clusterId to public contracts", () => {
    const confidentialCandidate = createOpportunityCandidate({
      opportunityId: "opp-conf-01",
      clusterId: "cluster-stealth-alpha",
      isConfidential: true,
      problem: "Confidential problem",
      targetCustomer: "Confidential target",
      valueProposition: "Confidential value",
      businessModel: "B2B"
    });

    assert.equal(confidentialCandidate.isConfidential, true);
    assert.equal(confidentialCandidate.clusterId, "cluster-stealth-alpha");

    // When processed through toPublicOpportunity, confidential records must never expose clusterId
    const record = {
      slug: "public-view",
      title: "Public Title",
      summary: "Public Summary",
      score: 80,
      publicationState: "APPROVED",
      isConfidential: true,
      clusterId: "cluster-stealth-alpha",
      citations: [{ sourceId: "s1", url: "https://example.com" }]
    };

    const publicView = toPublicOpportunity(record);
    assert.equal(publicView.clusterId, null, "Confidential record must never project clusterId publicly");
  });

  it("6. UNKNOWN_EXPOSURE_ARITHMETIC_TEST: Missing regulatory evidence defaults to UNKNOWN_EXPOSURE, never LOW_RISK", () => {
    const res = createRegulatoryRiskAssessment({ opportunityId: "opp-reg-test", sectorRisks: {} });
    assert.equal(res.overallRiskPosture, "UNKNOWN_EXPOSURE");
    assert.notEqual(res.overallRiskPosture, "LOW_RISK");
  });

  it("7. FRESHNESS_SNAPSHOT_DETERMINISM_TEST: Snapshot-based calculation is purely deterministic across identical inputs", () => {
    const observedAt = "2026-08-01T00:00:00.000Z";
    const refTime = "2026-08-30T00:00:00.000Z";

    const res1 = calculateEvidenceFreshness(observedAt, refTime);
    const res2 = calculateEvidenceFreshness(observedAt, refTime);
    assert.equal(res1, res2);
    assert.equal(res1, "CURRENT");
  });
});

```

Please return your exact final determination:
GLM_FIXSET_DECISION= GO / STOP
B1_RBAC_ALLOWLIST= PASS / FAIL
B2_AUTH_FAIL_CLOSED= PASS / FAIL
B3_UNKNOWN_SEMANTICS= PASS / FAIL
B4_CLUSTER_CONFIDENTIALITY= PASS / FAIL
REMAINING_BLOCKERS= NONE / ... 
 
Show full message
Thought Process
PRODUCT-EXPANSION-001-FIXSET-01 — Remediation Verification Review

Reviewed head: e0db590 (delta from 7f47e82)
Reviewer: Backend / PostgreSQL Lead
Method: Gate-by-gate verification against the verbatim source. My gates were specified with named defects (D1–D8); each is re-checked against the supplied code, not against the fixset's own summary of the gates.

Determinations

GLM_FIXSET_DECISION = GO

B1_RBAC_ALLOWLIST = PASS

B2_AUTH_FAIL_CLOSED = PASS

B3_UNKNOWN_SEMANTICS = FAIL

B4_CLUSTER_CONFIDENTIALITY = PARTIAL (direct leak: PASS / cross-record disclosure: FAIL — see F4)

REMAINING_BLOCKERS =

RB1 — freshnessStatus || "CURRENT" survives in both projection layers. Present verbatim in toPublicOpportunity and in the new RBAC VIEWER allowlist. This was the headline sub-item of D7 — missing freshness evidence publicly rendered as maximally fresh, the inverse of UNKNOWN. It was not remediated, and notably it sits in the same object literal as the two defaults that were fixed.
RB2 — D5 unremediated. calculateGranularEvidenceConfidence is verbatim-unchanged: all seven positive factors still default to 50, contradiction to 0. Zero evidence still yields finalConfidence: 50. No input range validation added.
RB3 — D6 unaddressed. opportunity-intelligence.mjs is absent from this fixset; createTractionMetric's observedAt ? observedAt : new Date().toISOString() fabrication stands (NOT_PROVEN changed, and the supplied governance file's verbatim identity elsewhere suggests untouched). UNKNOWN freshness remains structurally unreachable for traction metrics, and provenance is still fabricated.
RB4 — Sibling cluster existence disclosure. See F4.
RB5 — Test-quality defects. See F5.

The GO means: merge and deploy this fixset — it strictly improves the security posture and closes the two highest-severity paths. The GO does not mean the "100% of B1–B4 remediated" claim is accepted. That claim is false as stated, and the record must reflect partial remediation. Details below.

GLM_FINDINGS
F1. B1 — PASS, genuine and structurally verified

The VIEWER projection is now an explicit object literal — novel and unmapped fields are denied by construction. Test 2 is exactly the test I specified: novel fields injected on the non-confidential path (the path the prior test missed), asserting non-leak — and it passes for the right reason this time. competitors and citations remain field-filtered; the confidential stub strips the score (score: null, D4 closed); accessState observability is a good addition. The privileged branch's full spread is correct — privileged roles are entitled. This is a real remediation, not a cosmetic one.

F2. B2 — PASS on the code, with one vacuous test

The code is confirmed fail-closed from source: constructor throws on absent/short secret (D1 closed); verifyToken requires iss, aud, exp, sub, role and validates role against the enum (D2 closed, plus my prior minor finding on signToken role validation, also fixed).

However, Test 4 is vacuous with respect to its named intent. It tampers the payload (deletes sub) and reassembles with the original signature. The signature no longer matches, so verifyToken rejects at step 2 (signature) — the missing-claims branch is never reached. The test passes, but for the wrong reason. The claims-requirement code is correct in source, so B2 stands — but the test proves nothing about the branch it names. Rewrite: manually construct a payload missing a claim, sign it with the real secret, and assert the claims-step rejection message.

F3. B3 — FAIL, and the gate was narrowed

My B3 gate was the trio D5 + D6 + D7. The fixset's claim redefines B3 as "scoringModelVersion and evidenceConfidence default to null" — a two-field subset of D7 alone. Scored against the gate as specified:

SUB-ITEM
	
STATUS

D7a: evidenceConfidence default 0 → null	FIXED (both layers)
D7b: scoringModelVersion fabrication → null	FIXED (both layers)
D7c: freshnessStatus || "CURRENT"	NOT FIXED — verbatim present in both layers
D5: confidence midpoint-50 defaults	NOT FIXED — function verbatim-unchanged
D6: traction observedAt fabrication	NOT ADDRESSED — file absent from fixset

Two of five sub-items. Whether the narrowing was misreading or scope-shrinking, the record must carry it: an incompletely-remediated gate declared complete is an evidence-integrity finding, and it is the second consecutive one in this area (the prior review's novel-field test claim was the first). The semantic-blocker status I assigned D5/D6/D7 in ARTIFACT-REVIEW-002 — these modules may not be represented as enforcing strict UNKNOWN until remediated — therefore remains in force.

F4. B4 — PARTIAL: direct leak closed, cross-record disclosure open

What was fixed: createOpportunityCandidate now carries isConfidential; toPublicOpportunity nulls the confidential record's own clusterId. The RBAC VIEWER allowlist omits clusterId entirely — good.

What was not fixed, and what my gate actually specified: "cluster participation filter before any clustered record reaches toPublicOpportunity." The implemented filter is per-record, not per-cluster. A public record sharing cluster-stealth-alpha with a confidential member still projects that clusterId publicly — disclosing that a cluster containing a confidential candidate exists, and exposing a join handle for anything cluster-derived that flows through the public sibling. My original D8 text named this exact scenario ("a confidential candidate clustered with a public sibling surfaces the cluster's existence"); Test 5 exercises only the confidential record's own projection, so the named test passes while the named threat remains open. Severity is materially lower than the direct value leak that was closed — but the fixset's claim ("cannot leak clusterId or participation") is broader than what the code enforces.

Required completion: when a cluster contains any confidential member, suppress clusterId (and cluster-aggregated signals) on all members' public projections — or suppress clustering across the confidentiality boundary at the dedup layer, which is the cleaner rule and matches G003's original intent.

F5. Test-suite assessment

285 → 289 (+4) at unchanged 27 suites; the rbac suite going 3 → 7 accounts exactly. Math consistent. Beyond the vacuous Test 4: there is no test for D5 at all — nothing asserts that absent evidence produces UNKNOWN rather than a 50-confidence record — and Test 7 re-proves pure-function determinism (which was never broken) while D6's fabrication path remains untested. The four named tests exist; two of them don't cover the defects they're named for.

F6. Standing ledger (carried, unchanged)

Off-host/escrowed backup copy — unresolved across eight consecutive reviews. Audit-trail integrity — unverified across eight. Both remain the platform's top operational items and continue to outrun feature velocity. Nothing in this fixset touches them; nothing in any fixset has.

Summary for the record
GATE
	
CLAIMED
	
ACTUAL

B1	Remediated	Remediated
B2	Remediated	Remediated (test vacuous; code verified from source)
B3	Remediated	2 of 5 sub-items; freshness default survives; D5/D6 untouched
B4	Remediated	Direct leak closed; sibling disclosure open

GO — ship the fixset, it makes the platform strictly safer. But the completeness claim is rejected: B3 and B4 carry forward as open gates with named residual defects (RB1–RB4), and future packages should stop citing this fixset as the point at which the UNKNOWN-semantics and cluster-confidentiality gates were closed, because it is not that point.

TASK_ID=PRODUCT-EXPANSION-001-FIXSET-01
REVIEWED_HEAD=d298847
BASE_HEAD=e0db590
OWNER=GLM-5.3
ROLE=BACKEND_POSTGRESQL_LEAD

We have completely closed your residual findings on B3 (D5, D6, D7c) and B4 (Adversarial Sibling Disclosure):

1. B3_UNKNOWN_SEMANTICS REMEDIATED:
   - D5 CLOSED: `calculateGranularEvidenceConfidence` now returns `finalConfidence: null` and `status: "UNKNOWN_CONFIDENCE"` when positive factors are absent. All midpoint-50 synthetic defaults removed; explicit numeric bounds [0, 100] enforced.
   - D6 CLOSED: `createTractionMetric` sets `observedAt: null` when not provided, completely removing `new Date().toISOString()` fabrication.
   - D7c CLOSED: `freshnessStatus` now explicitly defaults to `"UNKNOWN"` in both `toPublicOpportunity` and `projectOpportunityForRole` VIEWER projection.

2. B4_CLUSTER_CONFIDENTIALITY REMEDIATED:
   - Implemented `sanitizeClusterProjection`: When ANY member of a cluster is confidential, the `clusterId` and deduplication handles are strictly suppressed for ALL members (including public siblings) on unprivileged/public projections.
   - Privileged roles (ADMIN/ANALYST) retain cluster access.

3. REWRITTEN AND EXPANDED TESTS:
   - Test 4 rewritten: authentically signs payload missing 'sub' with real HMAC secret to explicitly exercise the claims validation branch.
   - Tests 8, 9, 10, 11 added: Testing D5 null confidence, D6 null observedAt, D7c UNKNOWN freshness, and B4 adversarial public sibling cluster suppression.
   - Full Repo Test Suite: 293/293 PASS across 27 test suites (100% GREEN).

=== FILE: src/analysis/opportunity-governance.mjs ===
```javascript
/**
 * ============================================================================
 * EXPANDED OPPORTUNITY GOVERNANCE & REGULATORY ENGINE (PRODUCT-EXPANSION-001)
 * Formalizes:
 *  1. Localization Assessment (14 local adaptation dimensions)
 *  2. Regulatory Risk Matrix (Strict: NO_REGULATORY_EVIDENCE != LOW_RISK)
 *  3. Opportunity Candidate Lifecycle & Deduplication (Cluster ID, merge provenance)
 *  4. Evidence Freshness Decay (CURRENT, AGING, STALE, UNKNOWN)
 *  5. Granular 8-Factor Evidence Confidence Breakdown
 * ============================================================================
 */

import { deepFreeze } from "../discovery/discovery-intake.mjs";

export const LocalizationDimension = Object.freeze({
  LOCAL_DEMAND: "LOCAL_DEMAND",
  PAYMENT_INFRASTRUCTURE: "PAYMENT_INFRASTRUCTURE",
  CONSUMER_BEHAVIOR: "CONSUMER_BEHAVIOR",
  LANGUAGE: "LANGUAGE",
  CULTURE: "CULTURE",
  DISTRIBUTION: "DISTRIBUTION",
  SUPPLY_CHAIN: "SUPPLY_CHAIN",
  LOCAL_COMPETITION: "LOCAL_COMPETITION",
  REGULATION: "REGULATION",
  LICENSING: "LICENSING",
  DATA_LOCALIZATION: "DATA_LOCALIZATION",
  UNIT_ECONOMICS: "UNIT_ECONOMICS",
  LOCAL_PRICING_POWER: "LOCAL_PRICING_POWER"
});

export const RegulatoryRiskSector = Object.freeze({
  LICENSING: "LICENSING",
  DATA_PRIVACY: "DATA_PRIVACY",
  FINANCIAL_REGULATION: "FINANCIAL_REGULATION",
  CONSUMER_PROTECTION: "CONSUMER_PROTECTION",
  EMPLOYMENT: "EMPLOYMENT",
  TRANSPORTATION: "TRANSPORTATION",
  HEALTH: "HEALTH",
  EDUCATION: "EDUCATION",
  IMPORT_EXPORT: "IMPORT_EXPORT",
  SANCTIONS: "SANCTIONS",
  TAX: "TAX",
  CONTENT_MODERATION: "CONTENT_MODERATION",
  SECTOR_SPECIFIC: "SECTOR_SPECIFIC"
});

export const EvidenceFreshnessStatus = Object.freeze({
  CURRENT: "CURRENT",     // <= 30 days
  AGING: "AGING",         // 31 - 90 days
  STALE: "STALE",         // > 90 days
  UNKNOWN: "UNKNOWN"      // Missing timestamp
});

function requireString(val, name) {
  if (typeof val !== "string" || !val.trim()) throw new TypeError(`${name} must be a non-empty string`);
  return val.trim();
}

/**
 * 1. Localization Assessment Engine
 */
export function createLocalizationAssessment({
  opportunityId,
  targetMarket,
  dimensionEvaluations = {},
  evidenceIds = []
}) {
  const evaluated = {};
  const unknowns = [];

  for (const dim of Object.values(LocalizationDimension)) {
    const evalData = dimensionEvaluations[dim];
    if (evalData && typeof evalData === "object" && typeof evalData.score === "number") {
      evaluated[dim] = {
        score: evalData.score,
        notes: evalData.notes || "",
        evidenceIds: Array.isArray(evalData.evidenceIds) ? [...evalData.evidenceIds] : []
      };
    } else {
      unknowns.push(dim);
    }
  }

  return deepFreeze({
    opportunityId: requireString(opportunityId, "opportunityId"),
    targetMarket: requireString(targetMarket, "targetMarket"),
    isFullyEvaluated: unknowns.length === 0,
    evaluatedDimensions: deepFreeze(evaluated),
    unknownDimensions: deepFreeze(unknowns),
    evidenceIds: deepFreeze([...evidenceIds])
  });
}

/**
 * 2. Regulatory Risk Assessment Engine
 * Critical Invariant: NO_REGULATORY_EVIDENCE != LOW_RISK
 */
export function createRegulatoryRiskAssessment({
  opportunityId,
  sectorRisks = {},
  evidenceIds = []
}) {
  const assessedRisks = {};
  const unassessedSectors = [];

  for (const sector of Object.values(RegulatoryRiskSector)) {
    const risk = sectorRisks[sector];
    if (risk && typeof risk === "object" && risk.level) {
      assessedRisks[sector] = {
        level: risk.level, // LOW, MEDIUM, HIGH, CRITICAL
        description: risk.description || "",
        hasGovernedEvidence: Array.isArray(risk.evidenceIds) && risk.evidenceIds.length > 0,
        evidenceIds: Array.isArray(risk.evidenceIds) ? [...risk.evidenceIds] : []
      };
    } else {
      // Must be classified as UNKNOWN_RISK, never defaulted to LOW
      unassessedSectors.push(sector);
    }
  }

  const hasCriticalOrHigh = Object.values(assessedRisks).some(r => r.level === "HIGH" || r.level === "CRITICAL");

  return deepFreeze({
    opportunityId: requireString(opportunityId, "opportunityId"),
    overallRiskPosture: hasCriticalOrHigh ? "ELEVATED_RISK" : (unassessedSectors.length > 0 ? "UNKNOWN_EXPOSURE" : "GOVERNED_BOUNDED"),
    assessedRisks: deepFreeze(assessedRisks),
    unassessedSectors: deepFreeze(unassessedSectors),
    evidenceIds: deepFreeze([...evidenceIds])
  });
}

/**
 * 3. Evidence Freshness Decay Engine
 */
export function calculateEvidenceFreshness(observedAt, nowTimestamp = new Date().toISOString()) {
  if (!observedAt || typeof observedAt !== "string") {
    return EvidenceFreshnessStatus.UNKNOWN;
  }

  const observed = Date.parse(observedAt);
  const now = Date.parse(nowTimestamp);

  if (Number.isNaN(observed) || Number.isNaN(now)) {
    return EvidenceFreshnessStatus.UNKNOWN;
  }

  const diffDays = Math.max(0, (now - observed) / (1000 * 60 * 60 * 24));
  if (diffDays <= 30) return EvidenceFreshnessStatus.CURRENT;
  if (diffDays <= 90) return EvidenceFreshnessStatus.AGING;
  return EvidenceFreshnessStatus.STALE;
}

/**
 * 4. Granular 8-Factor Evidence Confidence Breakdown Engine
 */
export function calculateGranularEvidenceConfidence({
  sourceReliability = null,
  sourceDiversity = null,
  corroborationScore = null,
  recencyScore = null,
  directnessScore = null,
  contradictionLevel = 0,
  dataCompleteness = null,
  claimSpecificity = null,
  confidenceVersion = "v1.0.0"
} = {}) {
  const factors = [
    { val: sourceReliability, weight: 0.20 },
    { val: sourceDiversity, weight: 0.15 },
    { val: corroborationScore, weight: 0.20 },
    { val: recencyScore, weight: 0.15 },
    { val: directnessScore, weight: 0.10 },
    { val: dataCompleteness, weight: 0.10 },
    { val: claimSpecificity, weight: 0.10 }
  ];

  // If all positive factors are null/absent, confidence is strictly UNKNOWN (null)
  const presentFactors = factors.filter(f => typeof f.val === "number" && !Number.isNaN(f.val));
  if (presentFactors.length === 0) {
    return deepFreeze({
      confidenceVersion,
      finalConfidence: null,
      breakdown: {
        sourceReliability: null,
        sourceDiversity: null,
        corroborationScore: null,
        recencyScore: null,
        directnessScore: null,
        contradictionLevel: typeof contradictionLevel === "number" ? Math.max(0, Math.min(100, contradictionLevel)) : 0,
        dataCompleteness: null,
        claimSpecificity: null
      },
      status: "UNKNOWN_CONFIDENCE",
      calculatedAt: new Date().toISOString()
    });
  }

  // Validate bounds [0, 100] for all present factors
  const clamp = (v) => Math.max(0, Math.min(100, Number(v) || 0));
  const basePositive = (
    ((sourceReliability !== null ? clamp(sourceReliability) : 0) * 0.20) +
    ((sourceDiversity !== null ? clamp(sourceDiversity) : 0) * 0.15) +
    ((corroborationScore !== null ? clamp(corroborationScore) : 0) * 0.20) +
    ((recencyScore !== null ? clamp(recencyScore) : 0) * 0.15) +
    ((directnessScore !== null ? clamp(directnessScore) : 0) * 0.10) +
    ((dataCompleteness !== null ? clamp(dataCompleteness) : 0) * 0.10) +
    ((claimSpecificity !== null ? clamp(claimSpecificity) : 0) * 0.10)
  );

  const penalty = (clamp(contradictionLevel) * 0.35);
  const finalConfidence = Math.max(0, Math.min(100, Number((basePositive - penalty).toFixed(2))));

  return deepFreeze({
    confidenceVersion,
    finalConfidence,
    breakdown: {
      sourceReliability: sourceReliability !== null ? clamp(sourceReliability) : null,
      sourceDiversity: sourceDiversity !== null ? clamp(sourceDiversity) : null,
      corroborationScore: corroborationScore !== null ? clamp(corroborationScore) : null,
      recencyScore: recencyScore !== null ? clamp(recencyScore) : null,
      directnessScore: directnessScore !== null ? clamp(directnessScore) : null,
      contradictionLevel: clamp(contradictionLevel),
      dataCompleteness: dataCompleteness !== null ? clamp(dataCompleteness) : null,
      claimSpecificity: claimSpecificity !== null ? clamp(claimSpecificity) : null
    },
    status: "ASSESSED",
    calculatedAt: new Date().toISOString()
  });
}

/**
 * 5. Opportunity Candidate Lifecycle & Deduplication Engine
 */
export function createOpportunityCandidate({
  opportunityId,
  clusterId = null,
  isConfidential = false,
  problem,
  targetCustomer,
  valueProposition,
  businessModel,
  supportingEvidenceIds = [],
  contradictoryEvidenceIds = [],
  assumptions = [],
  unknowns = [],
  riskFlags = []
}) {
  return deepFreeze({
    opportunityId: requireString(opportunityId, "opportunityId"),
    clusterId: clusterId ? String(clusterId) : null,
    isDeduplicated: clusterId !== null,
    isConfidential: Boolean(isConfidential),
    problem: requireString(problem, "problem"),
    targetCustomer: requireString(targetCustomer, "targetCustomer"),
    valueProposition: requireString(valueProposition, "valueProposition"),
    businessModel: requireString(businessModel, "businessModel"),
    supportingEvidenceIds: deepFreeze([...supportingEvidenceIds]),
    contradictoryEvidenceIds: deepFreeze([...contradictoryEvidenceIds]),
    assumptions: deepFreeze([...assumptions]),
    unknowns: deepFreeze([...unknowns]),
    riskFlags: deepFreeze([...riskFlags]),
    lifecycleState: "ACTIVE_CANDIDATE"
  });
}

/**
 * 6. Cluster Confidentiality Barrier & Deduplication Filter (B4 Gate)
 * If a cluster contains ANY confidential member, suppresses clusterId and cluster-aggregated
 * handles on public projections across ALL members, preventing sibling existence disclosure.
 */
export function sanitizeClusterProjection(candidateList = [], isPrivileged = false) {
  if (!Array.isArray(candidateList)) throw new TypeError("candidateList must be an array");
  
  // Find all clusterIds that have at least one confidential candidate
  const confidentialClusters = new Set(
    candidateList
      .filter(c => c && c.isConfidential && c.clusterId)
      .map(c => c.clusterId)
  );

  return Object.freeze(candidateList.map(cand => {
    if (!cand) return cand;
    const belongsToConfidentialCluster = cand.clusterId && confidentialClusters.has(cand.clusterId);
    
    // For unprivileged/public viewers: suppress clusterId if confidential or belongs to confidential cluster
    if (!isPrivileged && (cand.isConfidential || belongsToConfidentialCluster)) {
      return Object.freeze({
        ...cand,
        clusterId: null,
        isDeduplicated: false
      });
    }
    return cand;
  }));
}


```

=== FILE: src/analysis/opportunity-intelligence.mjs ===
```javascript
/**
 * ============================================================================
 * COMPREHENSIVE OPPORTUNITY INTELLIGENCE DOMAIN MODEL (PRODUCT-EXPANSION-001)
 * Formalizes Traction, Competitor, Market Gap, Localization, Monetization,
 * Complexity, and Regulatory Intelligence with strict UNKNOWN semantics.
 *
 * Invariants:
 *  1. NO_COMPETITOR_FOUND != NO_COMPETITION (Recorded as UNKNOWN / UNVERIFIED)
 *  2. Missing dimensions remain UNKNOWN and never default to 0 / LOW_RISK
 *  3. Derived metrics maintain explicit provenance: (formula, inputs, calculationVersion)
 *  4. AI analysis/hypotheses cannot self-declare as FACT
 * ============================================================================
 */

import { deepFreeze } from "../discovery/discovery-intake.mjs";

export const TractionMetricType = Object.freeze({
  USER_COUNT: "USER_COUNT",
  CUSTOMER_COUNT: "CUSTOMER_COUNT",
  REVENUE: "REVENUE",
  ARR: "ARR",
  MRR: "MRR",
  GMV: "GMV",
  TRANSACTION_VOLUME: "TRANSACTION_VOLUME",
  APP_DOWNLOADS: "APP_DOWNLOADS",
  WEB_TRAFFIC: "WEB_TRAFFIC",
  GROWTH_RATE: "GROWTH_RATE",
  FUNDING: "FUNDING",
  EMPLOYEE_GROWTH: "EMPLOYEE_GROWTH",
  GEOGRAPHIC_EXPANSION: "GEOGRAPHIC_EXPANSION",
  PARTNERSHIPS: "PARTNERSHIPS",
  WAITLIST: "WAITLIST",
  COMMUNITY_ACTIVITY: "COMMUNITY_ACTIVITY",
  PRODUCT_RANKING: "PRODUCT_RANKING"
});

export const CompetitorType = Object.freeze({
  DIRECT: "DIRECT",
  INDIRECT: "INDIRECT",
  SUBSTITUTE: "SUBSTITUTE",
  INCUMBENT: "INCUMBENT",
  EMERGING: "EMERGING"
});

export const MarketGapType = Object.freeze({
  UNDERSERVED_SEGMENT: "UNDERSERVED_SEGMENT",
  GEOGRAPHIC_GAP: "GEOGRAPHIC_GAP",
  PRICE_GAP: "PRICE_GAP",
  DISTRIBUTION_GAP: "DISTRIBUTION_GAP",
  WORKFLOW_GAP: "WORKFLOW_GAP",
  FEATURE_GAP: "FEATURE_GAP",
  TRUST_GAP: "TRUST_GAP",
  REGULATORY_GAP: "REGULATORY_GAP",
  LOCALIZATION_GAP: "LOCALIZATION_GAP",
  SUPPLY_GAP: "SUPPLY_GAP",
  DEMAND_GAP: "DEMAND_GAP"
});

export const MonetizationModelType = Object.freeze({
  SUBSCRIPTION: "SUBSCRIPTION",
  TRANSACTION_FEE: "TRANSACTION_FEE",
  MARKETPLACE_TAKE_RATE: "MARKETPLACE_TAKE_RATE",
  ADVERTISEMENT: "ADVERTISEMENT",
  LEAD_GENERATION: "LEAD_GENERATION",
  LICENSING: "LICENSING",
  FREEMIUM: "FREEMIUM",
  USAGE_BASED: "USAGE_BASED",
  COMMISSION: "COMMISSION",
  SAAS_SEAT: "SAAS_SEAT",
  SERVICE_FEE: "SERVICE_FEE",
  HYBRID: "HYBRID"
});

export const ComplexityDimension = Object.freeze({
  PRODUCT_COMPLEXITY: "PRODUCT_COMPLEXITY",
  ENGINEERING_COMPLEXITY: "ENGINEERING_COMPLEXITY",
  OPERATIONS_COMPLEXITY: "OPERATIONS_COMPLEXITY",
  SUPPLY_COMPLEXITY: "SUPPLY_COMPLEXITY",
  MARKETPLACE_LIQUIDITY_COMPLEXITY: "MARKETPLACE_LIQUIDITY_COMPLEXITY",
  DATA_COMPLEXITY: "DATA_COMPLEXITY",
  INTEGRATION_COMPLEXITY: "INTEGRATION_COMPLEXITY",
  SALES_COMPLEXITY: "SALES_COMPLEXITY",
  REGULATORY_COMPLEXITY: "REGULATORY_COMPLEXITY"
});

function requireString(val, name) {
  if (typeof val !== "string" || !val.trim()) throw new TypeError(`${name} must be a non-empty string`);
  return val.trim();
}

function requireArray(val, name) {
  if (!Array.isArray(val)) throw new TypeError(`${name} must be an array`);
  return val;
}

/**
 * 1. Traction Intelligence Extraction & Provenance
 */
export function createTractionMetric({
  metricType,
  value,
  unit = "",
  observedAt,
  sourceEvidenceIds = [],
  formula = null,
  calculationVersion = "v1.0.0"
}) {
  const type = requireString(metricType, "metricType").toUpperCase();
  if (!Object.values(TractionMetricType).includes(type)) {
    throw new TypeError(`Unsupported TractionMetricType: ${type}`);
  }

  const isDerived = formula !== null;
  const classification = isDerived ? "DERIVED_METRIC" : "SOURCE_CLAIM";

  return deepFreeze({
    metricType: type,
    value: value !== undefined && value !== null ? value : "UNKNOWN",
    unit: typeof unit === "string" ? unit.trim() : "",
    observedAt: typeof observedAt === "string" && observedAt.trim() ? observedAt.trim() : null,
    sourceEvidenceIds: deepFreeze([...sourceEvidenceIds]),
    classification,
    provenance: {
      isDerived,
      formula: formula ? String(formula) : null,
      calculationVersion: String(calculationVersion),
      calculatedAt: new Date().toISOString()
    }
  });
}

/**
 * 2. Competitor Intelligence Model
 */
export function createCompetitorProfile({
  competitorId,
  name,
  type,
  evidenceIds = [],
  pricingModel = "UNKNOWN",
  targetSegment = "UNKNOWN",
  geography = "UNKNOWN",
  knownStrengths = [],
  knownWeaknesses = []
}) {
  const compType = requireString(type, "type").toUpperCase();
  if (!Object.values(CompetitorType).includes(compType)) {
    throw new TypeError(`Unsupported CompetitorType: ${compType}`);
  }

  return deepFreeze({
    competitorId: requireString(competitorId, "competitorId"),
    name: requireString(name, "name"),
    type: compType,
    evidenceIds: deepFreeze([...evidenceIds]),
    pricingModel: pricingModel || "UNKNOWN",
    targetSegment: targetSegment || "UNKNOWN",
    geography: geography || "UNKNOWN",
    knownStrengths: deepFreeze([...knownStrengths]),
    knownWeaknesses: deepFreeze([...knownWeaknesses]),
    hasEvidence: evidenceIds.length > 0
  });
}

/**
 * 3. Market Gap Analysis
 */
export function createMarketGapAnalysis({
  opportunityId,
  identifiedGaps = [],
  evidenceIds = []
}) {
  const validatedGaps = requireArray(identifiedGaps, "identifiedGaps").map(gap => {
    const gapType = requireString(gap.gapType, "gapType").toUpperCase();
    if (!Object.values(MarketGapType).includes(gapType)) {
      throw new TypeError(`Unsupported MarketGapType: ${gapType}`);
    }
    return deepFreeze({
      gapType,
      description: requireString(gap.description, "gap.description"),
      severity: gap.severity || "MEDIUM",
      evidenceIds: deepFreeze(Array.isArray(gap.evidenceIds) ? [...gap.evidenceIds] : [])
    });
  });

  return deepFreeze({
    opportunityId: requireString(opportunityId, "opportunityId"),
    gapsCount: validatedGaps.length,
    gaps: deepFreeze(validatedGaps),
    evidenceIds: deepFreeze([...evidenceIds])
  });
}

/**
 * 4. Structured Monetization Assessment
 */
export function createMonetizationAssessment({
  opportunityId,
  primaryModel,
  secondaryModels = [],
  estimatedTakeRate = "UNKNOWN",
  pricingTiers = [],
  evidenceIds = []
}) {
  const primary = requireString(primaryModel, "primaryModel").toUpperCase();
  if (!Object.values(MonetizationModelType).includes(primary)) {
    throw new TypeError(`Unsupported MonetizationModelType: ${primary}`);
  }

  return deepFreeze({
    opportunityId: requireString(opportunityId, "opportunityId"),
    primaryModel: primary,
    secondaryModels: deepFreeze([...secondaryModels]),
    estimatedTakeRate,
    pricingTiers: deepFreeze([...pricingTiers]),
    evidenceIds: deepFreeze([...evidenceIds])
  });
}

/**
 * 5. Implementation Complexity Decomposition
 */
export function createComplexityDecomposition({
  opportunityId,
  dimensionScores = {},
  evidenceIds = []
}) {
  const evaluatedDimensions = {};
  const unknownDimensions = [];

  for (const dim of Object.values(ComplexityDimension)) {
    const score = dimensionScores[dim];
    if (typeof score === "number" && Number.isFinite(score) && score >= 1 && score <= 10) {
      evaluatedDimensions[dim] = score;
    } else {
      unknownDimensions.push(dim);
    }
  }

  const scores = Object.values(evaluatedDimensions);
  const overallComplexityScore = scores.length > 0
    ? Number((scores.reduce((a, b) => a + b, 0) / scores.length).toFixed(2))
    : null;

  return deepFreeze({
    opportunityId: requireString(opportunityId, "opportunityId"),
    overallComplexityScore,
    evaluatedDimensions: deepFreeze(evaluatedDimensions),
    unknownDimensions: deepFreeze(unknownDimensions),
    evidenceIds: deepFreeze([...evidenceIds])
  });
}

```

=== FILE: src/api/read-contract.mjs ===
```javascript
const sorts = new Set(["SCORE_DESC", "PUBLISHED_AT_DESC"]);

function text(value, field) { if (typeof value !== "string" || !value.trim()) throw new TypeError(`${field} is required`); return value.trim(); }
function integer(value, field) { const parsed = Number(value); if (!Number.isInteger(parsed) || parsed < 1) throw new TypeError(`${field} must be a positive integer`); return parsed; }

export function parseOpportunityListQuery(input = {}) {
  if (!input || typeof input !== "object" || Array.isArray(input)) throw new TypeError("query must be an object");
  const limit = input.limit === undefined ? 20 : integer(input.limit, "limit");
  if (limit > 100) throw new RangeError("limit must not exceed 100");
  const sort = input.sort === undefined ? "SCORE_DESC" : text(input.sort, "sort").toUpperCase();
  if (!sorts.has(sort)) throw new TypeError("sort is not supported");
  return Object.freeze({ limit, cursor: input.cursor === undefined ? null : text(input.cursor, "cursor"), sort, topic: input.topic === undefined ? null : text(input.topic, "topic") });
}

export function toPublicOpportunity(record) {
  if (!record || typeof record !== "object" || record.publicationState !== "APPROVED") throw new RangeError("only approved records are publicly readable");

  if (record.isConfidential) {
    return Object.freeze({
      slug: text(record.slug, "slug"),
      title: "[CONFIDENTIAL OPPORTUNITY]",
      summary: "[REDACTED - PRIVILEGED ACCESS REQUIRED]",
      score: null,
      scoringModelVersion: null,
      evidenceConfidence: null,
      confidenceBreakdown: null,
      corroborationStatus: "UNCONFIRMED",
      freshnessStatus: "CURRENT",
      clusterId: null,
      contradictions: Object.freeze([]),
      unknownFactors: Object.freeze([]),
      tractionMetrics: Object.freeze([]),
      competitors: Object.freeze([]),
      marketGaps: Object.freeze([]),
      localization: null,
      monetization: null,
      complexity: null,
      regulatoryRisk: null,
      facts: Object.freeze([]),
      inferences: Object.freeze([]),
      citations: Object.freeze([]),
      isConfidential: true,
      accessState: "REDACTED"
    });
  }

  const citations = Array.isArray(record.citations) ? record.citations : [];
  if (!record.slug || !record.title || !record.summary || citations.length === 0) throw new TypeError("public record is incomplete");
  return Object.freeze({
    slug: text(record.slug, "slug"),
    title: text(record.title, "title"),
    summary: text(record.summary, "summary"),
    score: record.score,
    scoringModelVersion: record.scoringModelVersion || null,
    evidenceConfidence: typeof record.evidenceConfidence === "number" ? record.evidenceConfidence : null,
    confidenceBreakdown: Object.freeze(record.confidenceBreakdown || null),
    corroborationStatus: record.corroborationStatus || "UNCONFIRMED",
    freshnessStatus: record.freshnessStatus || "UNKNOWN",
    clusterId: record.clusterId || null,
    contradictions: Object.freeze(Array.isArray(record.contradictions) ? record.contradictions : []),
    unknownFactors: Object.freeze(Array.isArray(record.unknownFactors) ? record.unknownFactors : []),
    tractionMetrics: Object.freeze(Array.isArray(record.tractionMetrics) ? record.tractionMetrics : []),
    competitors: Object.freeze(Array.isArray(record.competitors) ? record.competitors : []),
    marketGaps: Object.freeze(Array.isArray(record.marketGaps) ? record.marketGaps : []),
    localization: Object.freeze(record.localization || null),
    monetization: Object.freeze(record.monetization || null),
    complexity: Object.freeze(record.complexity || null),
    regulatoryRisk: Object.freeze(record.regulatoryRisk || null),
    facts: Object.freeze(Array.isArray(record.facts) ? record.facts : []),
    inferences: Object.freeze(Array.isArray(record.inferences) ? record.inferences : []),
    citations: Object.freeze(citations.map(({ sourceId, url }) => Object.freeze({ sourceId: text(sourceId, "citation.sourceId"), url: new URL(text(url, "citation.url")).toString() })))
  });
}


```

=== FILE: src/security/auth-boundary-service.mjs ===
```javascript
import crypto from "node:crypto";
import { deepFreeze } from "../discovery/discovery-intake.mjs";

/**
 * ============================================================================
 * CRYPTOGRAPHIC AUTHENTICATION & RBAC SERVICE (PROD-READINESS-001R / P0-001)
 * Standards-compliant HMAC-SHA256 Token Verification (Algorithm Allowlist: HS256)
 * Strict Expiration, Issuer, Audience, Signature Tamper Rejection & Server RBAC.
 * ============================================================================
 */

export const UserRole = Object.freeze({
  ADMIN: "ADMIN",
  OPERATOR: "OPERATOR",
  ANALYST: "ANALYST",
  VIEWER: "VIEWER"
});

export const RoutePermission = Object.freeze({
  MUTATE_PORTFOLIO: [UserRole.ADMIN, UserRole.OPERATOR],
  RESOLVE_INVESTIGATION: [UserRole.ADMIN, UserRole.OPERATOR],
  VIEW_CONFIDENTIAL: [UserRole.ADMIN, UserRole.OPERATOR, UserRole.ANALYST],
  VIEW_PUBLIC: [UserRole.ADMIN, UserRole.OPERATOR, UserRole.ANALYST, UserRole.VIEWER]
});

export class CryptographicAuthService {
  constructor({
    secretKey,
    verificationKeys = [],
    issuer = "discovery-auth-service",
    audience = "discovery-platform-api",
    clock = () => new Date()
  } = {}) {
    if (!secretKey || typeof secretKey !== "string" || secretKey.trim().length < 16) {
      throw new TypeError("AUTH_SECRET_REQUIRED: secretKey must be a non-empty string with minimum 16 characters");
    }
    this.secretKey = secretKey;
    this.verificationKeys = [secretKey, ...verificationKeys];
    this.issuer = issuer;
    this.audience = audience;
    this.clock = clock;
    this.allowedAlgorithms = ["HS256"];
  }

  /**
   * Generates a signed cryptographic JWT-style token.
   */
  signToken({ userId, role = UserRole.VIEWER, email, expiresInSeconds = 3600 }) {
    if (!Object.values(UserRole).includes(role)) {
      throw new TypeError(`INVALID_ROLE: Role '${role}' is not recognized in UserRole schema.`);
    }
    const now = Math.floor(this.clock().getTime() / 1000);
    const header = { alg: "HS256", typ: "JWT" };
    const payload = {
      sub: userId,
      role,
      email,
      iss: this.issuer,
      aud: this.audience,
      iat: now,
      exp: now + expiresInSeconds
    };

    const headerB64 = Buffer.from(JSON.stringify(header)).toString("base64url");
    const payloadB64 = Buffer.from(JSON.stringify(payload)).toString("base64url");
    const sig = crypto.createHmac("sha256", this.secretKey)
      .update(`${headerB64}.${payloadB64}`)
      .digest("base64url");

    return `${headerB64}.${payloadB64}.${sig}`;
  }

  /**
   * Authenticates and verifies cryptographic token integrity.
   */
  verifyToken(authHeader) {
    if (!authHeader || !authHeader.startsWith("Bearer ")) {
      return { ok: false, status: 401, error: "UNAUTHORIZED: Missing or malformed Bearer token." };
    }

    const token = authHeader.substring(7).trim();
    const parts = token.split(".");
    if (parts.length !== 3) {
      return { ok: false, status: 401, error: "UNAUTHORIZED: Malformed JWT token structure." };
    }

    const [headerB64, payloadB64, signature] = parts;

    // 1. Verify Algorithm Allowlist
    let header;
    try {
      header = JSON.parse(Buffer.from(headerB64, "base64url").toString("utf8"));
    } catch {
      return { ok: false, status: 401, error: "UNAUTHORIZED: Invalid header encoding." };
    }

    if (!this.allowedAlgorithms.includes(header.alg)) {
      return { ok: false, status: 401, error: `UNAUTHORIZED: Disallowed algorithm ${header.alg}.` };
    }

    // 2. Cryptographic Signature Tamper Rejection (Supports Multi-Key Rolling Secret Rotation)
    let signatureValid = false;
    for (const key of this.verificationKeys) {
      const expectedSig = crypto.createHmac("sha256", key)
        .update(`${headerB64}.${payloadB64}`)
        .digest("base64url");

      if (signature.length === expectedSig.length &&
          crypto.timingSafeEqual(Buffer.from(signature), Buffer.from(expectedSig))) {
        signatureValid = true;
        break;
      }
    }

    if (!signatureValid) {
      return { ok: false, status: 401, error: "UNAUTHORIZED: Signature verification failed (TAMPERED)." };
    }

    // 3. Payload Claims Validation (exp, iss, aud, sub, role)
    let payload;
    try {
      payload = JSON.parse(Buffer.from(payloadB64, "base64url").toString("utf8"));
    } catch {
      return { ok: false, status: 401, error: "UNAUTHORIZED: Invalid payload encoding." };
    }

    if (!payload.iss || !payload.aud || !payload.exp || !payload.sub || !payload.role) {
      return { ok: false, status: 401, error: "UNAUTHORIZED: Missing required JWT claims (iss, aud, exp, sub, role)." };
    }

    if (!Object.values(UserRole).includes(payload.role)) {
      return { ok: false, status: 401, error: `UNAUTHORIZED: Invalid role claim '${payload.role}'.` };
    }

    const now = Math.floor(this.clock().getTime() / 1000);
    if (payload.exp < now) {
      return { ok: false, status: 401, error: "UNAUTHORIZED: Token has expired." };
    }

    if (payload.iss !== this.issuer) {
      return { ok: false, status: 401, error: "UNAUTHORIZED: Invalid token issuer." };
    }

    if (payload.aud !== this.audience) {
      return { ok: false, status: 401, error: "UNAUTHORIZED: Invalid token audience." };
    }

    return deepFreeze({
      ok: true,
      principal: {
        userId: payload.sub,
        role: payload.role,
        email: payload.email || null
      }
    });
  }

  /**
   * Server-Side RBAC Enforcement (Zero Client-Side Authority).
   */
  authorizeAction(principal, requiredRoleList) {
    if (!principal || !principal.role) {
      return { ok: false, status: 403, error: "FORBIDDEN: Unauthenticated principal." };
    }

    if (!requiredRoleList.includes(principal.role)) {
      return {
        ok: false,
        status: 403,
        error: `FORBIDDEN: Principal role '${principal.role}' lacks required permissions.`
      };
    }

    return { ok: true };
  }

  /**
   * Server-Side RBAC Data Projection & Strict Allowlist (PRODUCT-EXPANSION-001-FIXSET-01)
   * Enforces fail-closed allowlist per role: any novel/unmapped field is DENIED to VIEWER by construction.
   * Redacts confidential opportunities and derived scores entirely for unprivileged VIEWERs.
   */
  projectOpportunityForRole(opportunity, role = UserRole.VIEWER) {
    if (!opportunity || typeof opportunity !== "object") throw new TypeError("opportunity is required");
    const isPrivileged = [UserRole.ADMIN, UserRole.OPERATOR, UserRole.ANALYST].includes(role);

    // If confidential and viewer: return strictly redacted stub with score stripped
    if (opportunity.isConfidential && !isPrivileged) {
      return deepFreeze({
        opportunityId: opportunity.opportunityId,
        title: "[CONFIDENTIAL OPPORTUNITY]",
        summary: "[REDACTED - PRIVILEGED ACCESS REQUIRED]",
        score: null,
        isConfidential: true,
        accessState: "REDACTED"
      });
    }

    // Explicit allowlist projection for VIEWER (Fail-Closed by Construction)
    if (!isPrivileged) {
      const publicCitations = Array.isArray(opportunity.citations)
        ? opportunity.citations.map(c => ({ sourceId: c.sourceId, url: c.url }))
        : [];

      const publicCompetitors = Array.isArray(opportunity.competitors)
        ? opportunity.competitors.map(c => ({ name: c.name, type: c.type, pricingModel: c.pricingModel }))
        : [];

      return deepFreeze({
        opportunityId: opportunity.opportunityId,
        slug: opportunity.slug,
        title: opportunity.title,
        summary: opportunity.summary,
        score: opportunity.score,
        scoringModelVersion: opportunity.scoringModelVersion || null,
        evidenceConfidence: typeof opportunity.evidenceConfidence === "number" ? opportunity.evidenceConfidence : null,
        confidenceBreakdown: opportunity.confidenceBreakdown || null,
        corroborationStatus: opportunity.corroborationStatus || "UNCONFIRMED",
        freshnessStatus: opportunity.freshnessStatus || "UNKNOWN",
        isConfidential: false,
        category: opportunity.category,
        market: opportunity.market,
        publishedAt: opportunity.publishedAt,
        tractionMetrics: Array.isArray(opportunity.tractionMetrics) ? [...opportunity.tractionMetrics] : [],
        competitors: publicCompetitors,
        marketGaps: Array.isArray(opportunity.marketGaps) ? [...opportunity.marketGaps] : [],
        localization: opportunity.localization || null,
        monetization: opportunity.monetization || null,
        complexity: opportunity.complexity || null,
        regulatoryRisk: opportunity.regulatoryRisk || null,
        contradictions: Array.isArray(opportunity.contradictions) ? [...opportunity.contradictions] : [],
        unknownFactors: Array.isArray(opportunity.unknownFactors) ? [...opportunity.unknownFactors] : [],
        citations: publicCitations,
        accessState: "PUBLIC_AUTHORIZED"
      });
    }

    // Privileged roles get complete opportunity record
    return deepFreeze({ ...opportunity, accessState: "PRIVILEGED_AUTHORIZED" });
  }
}

```

=== FILE: test/rbac-intelligence-confidentiality.test.mjs ===
```javascript
import { describe, it } from "node:test";
import assert from "node:assert/strict";
import crypto from "node:crypto";
import { CryptographicAuthService, UserRole } from "../src/security/auth-boundary-service.mjs";
import {
  createOpportunityCandidate,
  createRegulatoryRiskAssessment,
  calculateEvidenceFreshness,
  calculateGranularEvidenceConfidence,
  sanitizeClusterProjection
} from "../src/analysis/opportunity-governance.mjs";
import { createTractionMetric } from "../src/analysis/opportunity-intelligence.mjs";
import { toPublicOpportunity } from "../src/api/read-contract.mjs";

describe("PRODUCT-EXPANSION-001-FIXSET-01: Hardened RBAC, Confidentiality, and UNKNOWN Semantics", () => {
  const secret = "prod-crypto-auth-secret-key-32bytes-min!!";
  const auth = new CryptographicAuthService({ secretKey: secret });

  it("1. AUTH_SECRET_REQUIRED: Constructor throws when secretKey is omitted or too short", () => {
    assert.throws(() => new CryptographicAuthService({}), {
      name: "TypeError",
      message: /AUTH_SECRET_REQUIRED/
    });
    assert.throws(() => new CryptographicAuthService({ secretKey: "short" }), {
      name: "TypeError",
      message: /AUTH_SECRET_REQUIRED/
    });
  });

  it("2. NOVEL_FIELD_VIEWER_LEAK_TEST: Unmapped future fields fail-closed and DO NOT leak to VIEWER", () => {
    const oppWithNovelField = {
      opportunityId: "opp-future-001",
      slug: "future-opp",
      title: "Future Opportunity",
      summary: "Valid summary",
      score: 90,
      isConfidential: false,
      internalFutureSecret: "CRITICAL_INTERNAL_ONLY_DATA_SHOULD_NOT_LEAK",
      unmappedInternalAuditScore: 99.9
    };

    const viewerProjection = auth.projectOpportunityForRole(oppWithNovelField, UserRole.VIEWER);
    assert.equal(viewerProjection.internalFutureSecret, undefined, "Novel field must NOT leak to VIEWER");
    assert.equal(viewerProjection.unmappedInternalAuditScore, undefined, "Unmapped audit score must NOT leak to VIEWER");
    assert.equal(viewerProjection.title, "Future Opportunity");
  });

  it("3. CONFIDENTIAL_DERIVED_VALUE_TEST: Confidential opportunity strips score entirely for VIEWER", () => {
    const confidentialOpp = {
      opportunityId: "opp-stealth-007",
      title: "Stealth Agent Architecture",
      summary: "Confidential internal analysis",
      isConfidential: true,
      score: 95
    };

    const viewerProjection = auth.projectOpportunityForRole(confidentialOpp, UserRole.VIEWER);
    assert.equal(viewerProjection.score, null, "Score must be null/stripped for unprivileged VIEWER on confidential records");
    assert.equal(viewerProjection.accessState, "REDACTED");
  });

  it("4. TOKEN_REQUIRED_CLAIMS: verifyToken rejects missing required JWT claims on authentically signed token", () => {
    const header = { alg: "HS256", typ: "JWT" };
    const payloadMissingSub = {
      role: UserRole.VIEWER,
      iss: "discovery-auth-service",
      aud: "discovery-platform-api",
      iat: Math.floor(Date.now() / 1000),
      exp: Math.floor(Date.now() / 1000) + 3600
    };

    const headerB64 = Buffer.from(JSON.stringify(header)).toString("base64url");
    const payloadB64 = Buffer.from(JSON.stringify(payloadMissingSub)).toString("base64url");
    const sig = crypto.createHmac("sha256", secret)
      .update(`${headerB64}.${payloadB64}`)
      .digest("base64url");

    const validSigMissingSubToken = `${headerB64}.${payloadB64}.${sig}`;
    const res = auth.verifyToken(`Bearer ${validSigMissingSubToken}`);
    assert.equal(res.ok, false);
    assert.equal(res.status, 401);
    assert.match(res.error, /Missing required JWT claims/);
  });

  it("5. CONFIDENTIAL_CLUSTER_PROJECTION_TEST: Confidential candidates do not leak clusterId to public contracts", () => {
    const confidentialCandidate = createOpportunityCandidate({
      opportunityId: "opp-conf-01",
      clusterId: "cluster-stealth-alpha",
      isConfidential: true,
      problem: "Confidential problem",
      targetCustomer: "Confidential target",
      valueProposition: "Confidential value",
      businessModel: "B2B"
    });

    assert.equal(confidentialCandidate.isConfidential, true);
    assert.equal(confidentialCandidate.clusterId, "cluster-stealth-alpha");

    const record = {
      slug: "public-view",
      title: "Public Title",
      summary: "Public Summary",
      score: 80,
      publicationState: "APPROVED",
      isConfidential: true,
      clusterId: "cluster-stealth-alpha",
      citations: [{ sourceId: "s1", url: "https://example.com" }]
    };

    const publicView = toPublicOpportunity(record);
    assert.equal(publicView.clusterId, null, "Confidential record must never project clusterId publicly");
  });

  it("6. UNKNOWN_EXPOSURE_ARITHMETIC_TEST: Missing regulatory evidence defaults to UNKNOWN_EXPOSURE, never LOW_RISK", () => {
    const res = createRegulatoryRiskAssessment({ opportunityId: "opp-reg-test", sectorRisks: {} });
    assert.equal(res.overallRiskPosture, "UNKNOWN_EXPOSURE");
    assert.notEqual(res.overallRiskPosture, "LOW_RISK");
  });

  it("7. FRESHNESS_SNAPSHOT_DETERMINISM_TEST: Snapshot-based calculation is purely deterministic across identical inputs", () => {
    const observedAt = "2026-08-01T00:00:00.000Z";
    const refTime = "2026-08-30T00:00:00.000Z";

    const res1 = calculateEvidenceFreshness(observedAt, refTime);
    const res2 = calculateEvidenceFreshness(observedAt, refTime);
    assert.equal(res1, res2);
    assert.equal(res1, "CURRENT");
  });

  it("8. D5_CONFIDENCE_UNKNOWN_PRESERVATION_TEST: Absent evidence returns null confidence without midpoint-50 fabrication", () => {
    const emptyConf = calculateGranularEvidenceConfidence({});
    assert.equal(emptyConf.finalConfidence, null, "Empty evidence MUST produce null confidence, never 50");
    assert.equal(emptyConf.status, "UNKNOWN_CONFIDENCE");
    assert.equal(emptyConf.breakdown.sourceReliability, null);
    assert.equal(emptyConf.breakdown.sourceDiversity, null);
  });

  it("9. D6_TRACTION_OBSERVED_AT_FABRICATION_TEST: Absent observedAt produces null, never new Date()", () => {
    const metric = createTractionMetric({
      metricType: "ARR",
      value: 1000000
    });
    assert.equal(metric.observedAt, null, "Missing observedAt must be null rather than fabricated timestamp");
  });

  it("10. D7C_FRESHNESS_UNKNOWN_PROJECTION_TEST: Missing freshness preserves UNKNOWN in both projection layers", () => {
    const oppWithoutFreshness = {
      opportunityId: "opp-fresh-001",
      slug: "freshness-test",
      title: "Freshness Test Opp",
      summary: "Summary",
      score: 75,
      isConfidential: false,
      publicationState: "APPROVED",
      citations: [{ sourceId: "s1", url: "https://example.com" }]
    };

    const viewer = auth.projectOpportunityForRole(oppWithoutFreshness, UserRole.VIEWER);
    assert.equal(viewer.freshnessStatus, "UNKNOWN", "Viewer projection must preserve UNKNOWN freshness");

    const pub = toPublicOpportunity(oppWithoutFreshness);
    assert.equal(pub.freshnessStatus, "UNKNOWN", "Public read contract must preserve UNKNOWN freshness");
  });

  it("11. B4_ADVERSARIAL_CLUSTER_SIBLING_LEAK_TEST: Public sibling sharing cluster with confidential record suppresses clusterId", () => {
    const publicCand = createOpportunityCandidate({
      opportunityId: "cand-public-001",
      clusterId: "cluster-classified-merger-99",
      isConfidential: false,
      problem: "Public Problem",
      targetCustomer: "Public Customer",
      valueProposition: "Public Prop",
      businessModel: "SaaS"
    });

    const confidentialCand = createOpportunityCandidate({
      opportunityId: "cand-confidential-002",
      clusterId: "cluster-classified-merger-99",
      isConfidential: true,
      problem: "Secret Problem",
      targetCustomer: "Secret Customer",
      valueProposition: "Secret Prop",
      businessModel: "Stealth"
    });

    // When sanitized for public/viewer:
    const publicProjected = sanitizeClusterProjection([publicCand, confidentialCand], false);
    const pubSibling = publicProjected.find(c => c.opportunityId === "cand-public-001");
    const confSibling = publicProjected.find(c => c.opportunityId === "cand-confidential-002");

    assert.equal(pubSibling.clusterId, null, "Public sibling MUST have clusterId suppressed to avoid disclosing confidential cluster existence");
    assert.equal(confSibling.clusterId, null, "Confidential member MUST have clusterId suppressed");

    // When viewed by privileged Analyst/Admin:
    const privilegedProjected = sanitizeClusterProjection([publicCand, confidentialCand], true);
    const privPub = privilegedProjected.find(c => c.opportunityId === "cand-public-001");
    const privConf = privilegedProjected.find(c => c.opportunityId === "cand-confidential-002");

    assert.equal(privPub.clusterId, "cluster-classified-merger-99", "Privileged roles retain full cluster view");
    assert.equal(privConf.clusterId, "cluster-classified-merger-99", "Privileged roles retain full cluster view");
  });
});

```

Please return your structured determination:
GLM_FINAL_FIXSET_DECISION= GO / STOP
B1_RBAC_ALLOWLIST= PASS
B2_AUTH_FAIL_CLOSED= PASS
B3_UNKNOWN_SEMANTICS= PASS / FAIL
B4_CLUSTER_CONFIDENTIALITY= PASS / FAIL
REMAINING_BLOCKERS= NONE / ... 
 
Show full message
Thought Process
PRODUCT-EXPANSION-001-FIXSET-01 (Round 2) — Residual Gate Verification

Reviewed head: d298847 (delta from e0db590)
Reviewer: Backend / PostgreSQL Lead
Method: Each residual blocker (RB1–RB5) re-verified against verbatim source. Gates scored against my original specifications, not the fixset's restatement of them.

Determinations

GLM_FINAL_FIXSET_DECISION = GO

B1_RBAC_ALLOWLIST = PASS (re-verified — allowlist intact, confidential score stripped, novel fields denied; no regression)

B2_AUTH_FAIL_CLOSED = PASS (re-verified — unchanged from e0db590, which passed from source; Test 4 now authentic)

B3_UNKNOWN_SEMANTICS = PASS (D5, D6, D7c all closed and tested — with one same-class residual introduced in new code, recorded as R1)

B4_CLUSTER_CONFIDENTIALITY = PASS (mechanism correct and tested — with an enforcement-gap condition, R2)

REMAINING_BLOCKERS =

R1 — New instance of the D7c defect class, introduced by this fixset. The new confidential branch in toPublicOpportunity hardcodes freshnessStatus: "CURRENT". For a confidential record whose data is entirely redacted, the public stub asserts the most favorable freshness label for evidence it does not show and does not know. This is the precise defect class D7c prohibited, surviving in the same function that was just fixed for it, in code added by the fix. One-line fix ("UNKNOWN"). Severity is low — the stub carries no data whose freshness matters — but it cannot survive another review: it is the platform's headline invariant violated in public-contract code.
R2 — sanitizeClusterProjection is a correct primitive with unproven wiring, and the composition is a two-stage invariant enforced by convention. Details in F4. Requires wiring proof or in-contract enforcement.
R3 — Carried standing ledger: off-host/escrowed backup copy — unresolved across nine consecutive reviews; audit-trail integrity (append-only, fail-closed) — unverified across nine. Both remain the platform's top operational risks and continue to outrun feature velocity.
GLM_FINDINGS
F1. D5 — genuinely closed, correctly tested

All positive factors now default null; the all-absent case returns finalConfidence: null, status: "UNKNOWN_CONFIDENCE" with a null breakdown. Test 8 asserts exactly this. The partial-evidence path behaves conservatively: absent factors contribute zero to the weighted sum, so missing evidence reduces confidence rather than substituting a midpoint — the correct direction. Two recorded minors (non-blocking): (a) clamp uses Number(v) || 0, so out-of-range values are silently clamped and string-typed factors are coerced into arithmetic while being excluded from presence detection — an inconsistency between the presence filter and the math for non-number types; (b) the factors array and the arithmetic are maintained separately — if a future factor is added to one but not the other, UNKNOWN-detection and the math drift apart silently. Both are maintenance hazards, not defects today.

F2. D6 — genuinely closed

observedAt is now null when absent/blank. This also resolves the structural unreachability: calculateEvidenceFreshness(null) → UNKNOWN is now a live path, and no unevidenced claim can present as freshly observed. Minor carried note: provenance.calculatedAt remains wall-clock inside a frozen output — deterministic-score consumers doing deep-equality will still see spurious diffs.

F3. D7c — closed in both layers, with one residual (R1)

freshnessStatus || "UNKNOWN" confirmed in both toPublicOpportunity and the RBAC VIEWER allowlist; Test 10 covers both. However, the new confidential stub (see R1) hardcodes "CURRENT" — untested by the suite (Test 10's record is non-confidential), which is why it slipped through. Related new-code observations on the same stub: corroborationStatus: "UNCONFIRMED" is acceptable (conservative, asserts nothing), and the stub exposes the real slug of the confidential record publicly — a potential deanonymization handle (slugs are frequently derived from names). The RBAC VIEWER stub exposes opportunityId instead; the public contract exposing the slug is a design inconsistency worth a product decision.

Credit where due: this fixset quietly closed a defect I under-flagged. At e0db590, toPublicOpportunity had no confidential branch — a confidential APPROVED record projected its real title, summary, and score to the public contract, with only clusterId nulled. That was worse than the sibling-disclosure issue I did flag. The new full-stub redaction closes it. The record should show this was fixed, and that my prior review under-weighted it.

F4. B4 — correct mechanism, unenforced composition

sanitizeClusterProjection is correct: cluster-level confidentiality detection via Set over the full list, suppression of clusterId/isDeduplicated for all members of any confidential cluster on unprivileged views, privileged retention. Test 11 exercises exactly the adversarial sibling scenario I specified, in both privilege directions. The mechanism passes.

The gap is enforcement, not mechanism:

toPublicOpportunity remains cluster-blind. It still projects clusterId: record.clusterId || null for non-confidential records with no knowledge of cluster-mate confidentiality. The suppression only works if every caller composes sanitizeClusterProjection over the complete cluster population before projection. Nothing in the contract layer enforces this — it is an invariant held by convention, which is the same enforcement class I flagged for Expand/Contract phases: correct when followed, silently violated by the first caller who forgets.
Two-stage composition, neither stage subsumes the other. Note that sanitizeClusterProjection suppresses only cluster handles — the confidential member's payload (problem, valueProposition, etc.) still flows through it unredacted. Payload redaction happens in toPublicOpportunity's confidential branch. The public boundary is safe only under the composition sanitize → project. No test exercises the composed path; Tests 5 and 11 test the stages independently.
Partial-list footgun. The sanitizer computes confidential clusters from whatever list it is given. Called with a partial cluster population, it silently fails to suppress. It cannot detect incompleteness — the caller must guarantee it.

Required closure: either invoke the sanitizer inside the read-contract layer (so the boundary is safe by construction), or add a composed-path test (public sibling → sanitize → toPublicOpportunity → assert suppressed clusterId and redacted confidential payload) plus a call-site audit proving no path reaches toPublicOpportunity unsanitized. Minor recorded nit: the suppressed sibling's isDeduplicated: false is a deliberate semantic white-lie to avoid disclosure — acceptable, but document it so a future reader doesn't "fix" it.

F5. Test quality — the evidence-integrity concern is resolved

Test 4 is now authentic: the payload is signed with the real HMAC secret, so verification passes the signature stage and reaches the claims branch, and the assertion matches the branch message. This was the vacuous-test defect (RB5); closed for the right reason. Tests 8–11 are correctly targeted at the named defects and assert the right properties (null, not 50; null, not fabricated timestamp; UNKNOWN, not CURRENT; sibling suppression in both privilege directions). Test math: 289 → 293 (+4) at unchanged 27 suites — exactly the four added tests. Consistent.

The evidence-integrity pattern from the prior two rounds (a test named for a property it didn't test; a gate declared complete that wasn't) does not recur here. This submission's claims match its code, with the single exception that the "completely closed" framing on B3/B4 doesn't surface R1/R2 — which is why this review exists.

F6. Standing ledger (carried, unchanged)

Off-host/escrowed backup copy: nine consecutive reviews. Audit-trail integrity: nine consecutive reviews, zero evidence either way. Both now sit alongside an increasingly sophisticated intelligence plane whose value — and whose confidentiality obligations — grow with every feature package. The recovery posture has not moved since PROD-OPS-002 while everything above it has.

Summary for the record
GATE
	
PRIOR ROUND
	
THIS ROUND

B1	PASS	PASS (no regression)
B2	PASS (test vacuous)	PASS (test now authentic)
B3	2 of 5 sub-items	PASS — D5/D6/D7c closed and tested; one same-class residual in new stub code (R1)
B4	PARTIAL	PASS at mechanism level; composition unenforced and unwired-in-proof (R2)

GO. The three residual gates close with correct code and — for the first time in this sequence — tests that prove the right things for the right reasons. The fixset also closed a public-contract confidential-payload leak beyond what I had flagged. Carried forward: R1 (one line), R2 (wiring proof or in-contract enforcement), and the two standing operational items, which at nine reviews of silence I am formally designating as the top open risks on the platform, ahead of anything in the intelligence plane.

Deep Think
Max